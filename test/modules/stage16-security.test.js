import test from 'node:test';import assert from 'node:assert/strict';import {validateStage16ItemPlacementDraft,validateStage16ItemPlacementAudit} from '@rus/new-game/stages/stage-16/compat';import {makeStage16Input,makeStage16Draft,makeStage16Audit} from '../fixtures/stage13-16-fixtures.mjs';
for(const [key,code] of [['new_npcs','ITEM_PLACEMENT_CREATED_NPC'],['new_anchors','ITEM_PLACEMENT_CREATED_G5_ANCHOR'],['intro_prose','ITEM_PLACEMENT_CREATED_INTRO_PROSE'],['hidden_event','ITEM_PLACEMENT_CREATED_HIDDEN_EVENT'],['future_plot_item','ITEM_PLACEMENT_PLAYER_DESIRE_MATERIALIZED']])test(`Stage 16 rejects ${key}`,()=>{const d=makeStage16Draft({[key]:key==='intro_prose'?'x':{id:'x'}});assert.ok(validateStage16ItemPlacementDraft(d,makeStage16Input()).some(x=>x.code===code));});
test('Stage 16 audit cannot mutate draft',()=>{const a=makeStage16Audit();a.modified_draft={};assert.ok(validateStage16ItemPlacementAudit(a,makeStage16Draft(),makeStage16Input()).some(x=>x.code==='ITEM_PLACEMENT_AUDIT_MUTATED_OUTPUT'));});
