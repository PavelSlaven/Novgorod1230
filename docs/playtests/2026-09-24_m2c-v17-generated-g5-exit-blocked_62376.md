# M2c v17: UI-повтор после видимости локальных рёбер — run 62376

**BLOCKED на переходе к generated G5.** Обычный game-web в Chrome,
production HTTP handler и изолированная PostgreSQL с test approvals.
Production БД не использовалась. Party `party:2ed55a0c006d3def5affe56d`.
Прогон выполнен после `563df241`, который передаёт видимые локальные рёбра
в контекст хода.

Первый экран лесного старта, дословная проза:

```text
НА ПОДХОДЕ К ЛЕСНОЙ ГРЯДЕ

Вы — Микула, лесной промысловик. Тело не знает ран, и в нём достаточно сил для работы, а сильный голод не тревожит. На ногах низкая кожаная обувь, на теле нижняя рубаха и штаны. Всё это — то, что вы знаете о себе прямо сейчас, в этом месте, в этом времени.
```

В UI отправлено ровно `Осматриваюсь вокруг, оставаясь на месте.` Ход показал
«Вы внимательно изучили обстановку.» Party `state_version` 0→1, clock
`whole_minutes` 261120→261121. Точный повтор HTTP запроса вернул тот же
`screen_digest` `e6e56121e848c59dd56faec0d11360da0f452af9d66ab5a20fcf817e900448ae`
без нового хода. Reload и «Продолжить» показали ту же прозу.

Вкладка «Путь» снова показала «Других известных направлений нет»:
`screen.panels.route.data.movement.options=[]`; сохранённый
`visible_context.visible_objects` содержал только три надетые вещи.
Переход в generated G5 не выполнялся без раскрытой метки. Изолированная
fixture завершилась **0/1 FAIL**: `2 !== 5` обязательных шагов UI.

Отдельно выявлен другой барьер для occupied scene: точка `focus` имеет
capacity 1 и может быть занята NPC. Это не объясняет пустую вкладку «Путь»;
исправление данных о вместимости проходит отдельное утверждение.
