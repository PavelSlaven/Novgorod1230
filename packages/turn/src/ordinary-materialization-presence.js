import { deepFreeze } from '@rus/kernel';
import { assertOrdinaryMaterializationRequestV1, validateOrdinaryMaterializationPlanV1 } from '@rus/contracts/ordinary-materialization-v1';
import { assertAndNormalizeOrdinaryMaterializationWorkingProjection, canonicalDigest, createOrdinaryCandidateKey, createOrdinaryCategoryKey, createOrdinaryContextVersion, createOrdinaryCoverageKey, createOrdinaryResolutionRef, refreshOrdinaryMaterializationWorkingProjection, validateSupportingBasisAdmission } from '@rus/materialization';
import { turnFailure } from './errors.js';

export async function resolveOrdinaryMaterializationPresence({ envelope, ordinaryMaterializationModel, workingProjection, basisCatalog } = {}) {
  const input = envelopeOf(envelope), projection = projectionOf(input.request, workingProjection);
  const early = preflight(input, projection, basisCatalog); if (early) return early;
  if (typeof ordinaryMaterializationModel !== 'function') fail('TURN_ORDINARY_PRESENCE_MODEL_MISSING');
  const request = freeze(input.request); let raw = await invoke(ordinaryMaterializationModel, request, { repair: null }, false);
  let errors = validateOrdinaryMaterializationPlanV1(raw, request), repaired = false;
  if (errors.length) { raw = await invoke(ordinaryMaterializationModel, request, { repair: { schema: 'ordinary_materialization_repair_context_v1', original_output: null, validation_errors: errors } }, true); errors = validateOrdinaryMaterializationPlanV1(raw, request); repaired = true; if (errors.length) throw turnFailure('TURN_ORDINARY_PRESENCE_PLAN_INVALID', 'Ordinary presence response and its repair are invalid.', { validation_errors: errors }); }
  const plan = freeze(raw);
  if (plan.resolution !== 'materialize') return negative(input, plan, projection, repaired);
  const pending = positive(input, plan, projection, basisCatalog);
  return deepFreeze({ status: 'pending_items_property_admission', decision: decision(request, plan, repaired), pending_items_property_admission: pending, working_projection: projection });
}

function preflight(input, projection, bases) {
  const { request, identity } = input, aggregate = projection.ordinary_aggregate;
  const known = aggregate.presence_resolutions.find((r) => r.candidate_key === identity.candidate_key && r.coverage_key === identity.coverage_key && r.context_version === identity.context_version) ?? aggregate.coverage_closures.find((r) => r.coverage_key === identity.coverage_key && r.category_key === identity.category_key && r.context_version === identity.context_version);
  if (known) return deepFreeze({ status: 'already_resolved', decision: null, pending_items_property_admission: null, known_resolution: known, working_projection: projection });
  if (!fresh(input, aggregate)) return outcome('no_change', projection, 'aggregate_not_current');
  if (request.ordinary_state.closed_observation_scopes.includes(identity.coverage_key)) return outcome('no_change', projection, 'observation_closed');
  if (aggregate.remaining_identity_budget < 1 || aggregate.presence_resolutions.length + aggregate.coverage_closures.length >= aggregate.resolution_record_cap) return outcome('no_change', projection, 'budget_or_cap_exhausted');
  if (!propertyOK(input) || !input.property_placement_context.placement_catalog.some((v) => placementOK(v, request.scope_ref))) return outcome('no_change', projection, 'committed_property_or_placement_missing');
  if (!compatible(input, bases)) return outcome('authority_required', projection, 'supporting_basis_missing');
  return null;
}
function positive(input, plan, projection, bases) {
  const { request, identity } = input, entity = plan.entities[0];
  if (plan.entities.length !== 1 || entity.authority_class !== 'ordinary' || entity.admission_class !== 'common_mundane' || entity.availability_class !== 'common' || entity.functional_bucket !== identity.functional_bucket) reject('ORDINARY_PRESENCE_ENTITY_INVALID');
  if (!propertyOK(input) || entity.property_basis_ref !== request.context_refs.property_context_ref) reject('ORDINARY_PRESENCE_PROPERTY_INVALID');
  if (entity.placement_proposal.scope_ref !== request.scope_ref.entity_id || !input.property_placement_context.placement_catalog.some((v) => placementOK(v, request.scope_ref) && v.position_ref === entity.placement_proposal.position_ref)) reject('ORDINARY_PRESENCE_PLACEMENT_INVALID');
  try { validate(entity.supporting_basis_ref); for (const ref of entity.causal_basis.basis_refs) validate(ref); } catch (error) { reject('ORDINARY_PRESENCE_BASIS_INVALID', message(error)); }
  function validate(supporting_basis_ref) { validateSupportingBasisAdmission({ request, candidate: { supporting_basis_ref, functional_bucket: entity.functional_bucket, admission_class: entity.admission_class, availability_class: entity.availability_class, permission_refs: [] }, basis_catalog: bases }); }
  return deepFreeze({ schema: 'ordinary_pending_items_property_admission_v1', status: 'pending_items_property_admission', stage: 'presence_resolution', request_id: request.request_id, scope_ref: freeze(request.scope_ref), candidate_key: identity.candidate_key, coverage_key: identity.coverage_key, context_version: identity.context_version, admission_evidence: freeze({ authority_class: entity.authority_class, admission_class: entity.admission_class, availability_class: entity.availability_class, functional_bucket: entity.functional_bucket, supporting_basis_ref: entity.supporting_basis_ref, property_basis_ref: entity.property_basis_ref, runtime_item_mechanics_policy_ref: request.policy_refs.runtime_item_mechanics_policy_ref, property_placement_context_digest: input.property_placement_context_digest, property_catalog_version_ref: input.property_placement_context.property_catalog_version_ref, placement_catalog_version_ref: input.property_placement_context.placement_catalog_version_ref }), proposed_item: freeze(entity) });
}
function negative(input, plan, projection, repaired) { const { request, identity } = input; const transition = { kind: 'resolve_presence', request_identity: request.request_id, expected_state_version: projection.ordinary_aggregate.state_version, resolution_ref: createOrdinaryResolutionRef({ scope_ref: request.scope_ref, candidate_key: identity.candidate_key, coverage_key: identity.coverage_key, context_version: identity.context_version, request_identity: request.request_id, policy_version: identity.policy_version }), candidate_key: identity.candidate_key, coverage_key: identity.coverage_key, category_key: identity.category_key, context_version: identity.context_version, resolution: plan.resolution }; try { return deepFreeze({ status: plan.resolution, decision: decision(request, plan, repaired), pending_items_property_admission: null, working_projection: refreshOrdinaryMaterializationWorkingProjection({ working_projection: projection, ordinary_transition: transition }) }); } catch (error) { throw turnFailure('TURN_ORDINARY_PRESENCE_TRANSITION_INVALID', 'Ordinary transition cannot apply.', { cause: message(error) }); } }
function envelopeOf(value) { if (!jsonData(value)) fail('TURN_ORDINARY_PRESENCE_ENVELOPE_INVALID'); const e = record(value, ['schema','request','identity','ordinary_state_version','property_placement_context','property_placement_context_digest']); if (!e || e.schema !== 'ordinary_materialization_presence_envelope_v1') fail('TURN_ORDINARY_PRESENCE_ENVELOPE_INVALID'); try { assertOrdinaryMaterializationRequestV1(e.request); } catch { fail('TURN_ORDINARY_PRESENCE_ENVELOPE_INVALID'); } const i = record(e.identity, ['candidate_key','coverage_key','category_key','context_version','semantic_type','coverage_kind','coverage_ref','policy_version','functional_bucket','admission_class','availability_class']), p = record(e.property_placement_context, ['scope_ref','item_kind','property_catalog_version_ref','placement_catalog_version_ref','personal_communal_refs','occupied_site_refs','unowned_cause_refs','placement_context_refs','property_catalog','placement_catalog']); if (!i || !Object.values(i).every((v) => typeof v === 'string' && v) || !Number.isSafeInteger(e.ordinary_state_version) || e.request.mode !== 'resolve_presence' || e.request.candidate_query.evidence_weight !== 0 || !p || p.item_kind !== 'man_made' || !scope(p.scope_ref,e.request.scope_ref) || e.property_placement_context_digest !== canonicalDigest({domain:'rus.items.ordinary_world_property_placement_context.v1',...p})) fail('TURN_ORDINARY_PRESENCE_ENVELOPE_INVALID'); try { const candidate_key=createOrdinaryCandidateKey({ scope_ref:e.request.scope_ref,semantic_type:i.semantic_type,functional_bucket:i.functional_bucket,admission_class:i.admission_class,availability_class:i.availability_class,policy_version:i.policy_version }), coverage_key=createOrdinaryCoverageKey({scope_ref:e.request.scope_ref,coverage_kind:i.coverage_kind,coverage_ref:i.coverage_ref,policy_version:i.policy_version}), category_key=createOrdinaryCategoryKey({scope_ref:e.request.scope_ref,functional_bucket:i.functional_bucket,admission_class:i.admission_class,availability_class:i.availability_class,policy_version:i.policy_version}), context_version=createOrdinaryContextVersion({scope_ref:e.request.scope_ref,context_refs:e.request.context_refs,ordinary_presence_policy_ref:e.request.policy_refs.ordinary_presence_policy_ref,property_basis_ref:e.request.context_refs.property_context_ref,property_placement_context_digest:e.property_placement_context_digest}); if (i.candidate_key!==candidate_key || i.coverage_key!==coverage_key || i.category_key!==category_key || i.context_version!==context_version || i.policy_version!==e.request.policy_refs.ordinary_presence_policy_ref || i.admission_class!=='common_mundane' || i.availability_class!=='common') fail('TURN_ORDINARY_PRESENCE_ENVELOPE_INVALID'); } catch { fail('TURN_ORDINARY_PRESENCE_ENVELOPE_INVALID'); } return freeze(e); }
function projectionOf(request, value) { let p; try { p = assertAndNormalizeOrdinaryMaterializationWorkingProjection(value); } catch { fail('TURN_ORDINARY_PRESENCE_WORKING_PROJECTION_INVALID'); } if (!scope(p.ordinary_aggregate.scope_ref, request.scope_ref)) fail('TURN_ORDINARY_PRESENCE_SCOPE_MISMATCH'); return p; }
function propertyOK(input) { return scope(input.property_placement_context.scope_ref, input.request.scope_ref) && input.property_placement_context.item_kind === 'man_made'; }
function fresh(input, aggregate) { const state=input.request.ordinary_state; return input.ordinary_state_version===aggregate.state_version && aggregate.seeded===state.seeded && aggregate.density_band===state.density_band && aggregate.remaining_identity_budget===state.remaining_identity_budget && sameRefs(aggregate.background_groups.map((g)=>g.group_ref),state.background_groups) && sameRefs(aggregate.presence_resolutions.map((r)=>r.resolution_ref),state.presence_resolutions) && aggregate.coverage_closures.every((r)=>state.closed_observation_scopes.includes(r.coverage_key)); }
function sameRefs(a,b) { return Array.isArray(b) && a.length===b.length && a.every((v,i)=>v===b[i]); }
function placementOK(v, s) { return v && v.state === 'committed' && scope(v.scope_ref, s); }
function compatible(input, bases) { if (!Array.isArray(bases)) return false; return bases.some((b) => { const p = record(b, Object.getOwnPropertyNames(b)); if (!p) return false; try { validateSupportingBasisAdmission({ request: input.request, candidate: { supporting_basis_ref: p.basis_ref, functional_bucket: input.identity.functional_bucket, admission_class: input.identity.admission_class, availability_class: input.identity.availability_class, permission_refs: [] }, basis_catalog: bases }); return true; } catch { return false; } }); }
function record(v, keys) { if (!v || typeof v !== 'object' || Array.isArray(v) || Object.getPrototypeOf(v) !== Object.prototype || Object.getOwnPropertySymbols(v).length) return null; const n = Object.getOwnPropertyNames(v); if (n.length !== keys.length || keys.some((k) => !n.includes(k))) return null; const out = {}; for (const k of keys) { const d = Object.getOwnPropertyDescriptor(v,k); if (d?.enumerable !== true || !Object.hasOwn(d,'value')) return null; out[k]=d.value; } return out; }
function jsonData(value, seen = new Set()) { if (value === null || typeof value === 'string' || typeof value === 'boolean' || (typeof value === 'number' && Number.isFinite(value))) return true; if (!value || typeof value !== 'object' || seen.has(value) || Object.getOwnPropertySymbols(value).length) return false; const array = Array.isArray(value); if (array ? Object.getPrototypeOf(value) !== Array.prototype : Object.getPrototypeOf(value) !== Object.prototype) return false; seen.add(value); for (const key of Object.getOwnPropertyNames(value)) { if (array && key === 'length') continue; const descriptor = Object.getOwnPropertyDescriptor(value,key); if (descriptor?.enumerable !== true || !Object.hasOwn(descriptor,'value') || !jsonData(descriptor.value,seen)) return false; } seen.delete(value); return true; }
function scope(a,b) { const x=record(a,['entity_kind','entity_id']), y=record(b,['entity_kind','entity_id']); return !!x && !!y && x.entity_kind===y.entity_kind && x.entity_id===y.entity_id; }
function outcome(status, working_projection, reason) { return deepFreeze({ status, decision:null, pending_items_property_admission:null, reason, working_projection }); }
async function invoke(model, request, context, repair) { try { return await model(request, freeze(context)); } catch (e) { throw turnFailure('TURN_ORDINARY_PRESENCE_MODEL_FAILED', repair ? 'Repair failed.' : 'Model failed.', { cause:message(e) }); } }
function decision(request, plan, repaired) { return deepFreeze({ schema:'ordinary_presence_resolution_decision_v1',request_id:request.request_id,scope_ref:freeze(request.scope_ref),resolution:plan.resolution,repaired }); }
function freeze(v) { return deepFreeze(structuredClone(v)); } function fail(code) { throw turnFailure(code, 'Stage B requires an exact committed server envelope.'); } function reject(code, m=code) { throw turnFailure('TURN_ORDINARY_PRESENCE_PLAN_REJECTED',m,{code}); } function message(e) { return e instanceof Error ? e.message : String(e); }
