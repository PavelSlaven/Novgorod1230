# VERIFICATION — history-events-knowledge

- Кто: независимый verifier agent (не автор; label `verify-history-events-knowledge`).
- Когда: 2026-09-26.
- Что проверено: `historical_events/events.csv`, `historical_events/event_phases.csv`,
  `historical_figures/figures.csv`, `knowledge_rumors/knowledge_rumors.csv`,
  `polities_external_relations/polities_external_relations.csv`, README всех доменов, скрипты `scripts/`.
- Источники сверки: черновик `nov_region_audit/novgorod_historical_timeline_1230_1250_v1.json`,
  `novgorod_rumor_templates_v1.json`, `novgorod_common_knowledge_v1.json`, `novgorod_route_knowledge_rules_v1.json`;
  sqlite `C:/Users/Slaven/Downloads/novgorod_1230(1) (1).sqlite` (persons_1230, events, birchbark_selection,
  territories, sources); книжные свидетельства `servak:/srv/novgorod-work/data/books/evidence/history-events-knowledge.csv`
  (349 строк, прошли независимую проверку; только чтение); URL из `source_refs` (факты Wikipedia сверены по
  общеизвестной хронологии, повторный WebFetch не делался).
- Данные не правились. Побочный эффект: при запуске `validate_events.js` (копия `.cjs`) файл
  `historical_events/rejected_report.json` перезаписан тем же содержимым `[]` (изменилось только mtime).

## Проверки скриптом (факт)

| Файл | Строк (скрипт) | README | Пустые source_refs | Пустые confidence | Дубли id | Confidence |
|---|---|---|---|---|---|---|
| events.csv | 35 | 35 | **нет колонки** (только `source_titles`) | 0 | 0 | C 35 |
| event_phases.csv | 175 | 175 | 0 | 0 | 0 (`phase_id`) | B 25, C 150 |
| figures.csv | 21 | 21 | 0 | 0 | 0 | A 14, C 6, D 1 |
| knowledge_rumors.csv | 105 | 105 (53/27/8/17) | 0 | 0 | 0 | C 80, A 17, B 8 |
| polities_external_relations.csv | 26 | 26 | 0 | 0 | 0 | C 13, B 8, A 3, A/B 1, D 1 |

- Скрипты `scripts/*.js` написаны на CommonJS (`require`), а `Novgorod-game-base/package.json` задаёт
  `"type": "module"`. Поэтому **все validate/build-скрипты падают на месте** с `ReferenceError: require is not defined`.
  Команды пересборки из README не воспроизводимы. Копии `.cjs` в scratch отработали и дали те же числа, что заявлены
  (175/0/0/175 unresolved v17; 21/0/3 перекрытия; 105/0/0).
- Точность переноса из черновика (скрипт): 175/175 фаз совпадают с `timeline[]` по `phase_id`, дате, названию
  и `source_date.raw`; 53/53 слуха совпадают с `rumor_templates` по названию, типу и образцам текста;
  16/16 и 11/11 профилей знания присутствуют.
- Шаблонность фаз (скрипт): у всех 175 фаз одинаковые `effect_roads`, `effect_market`, `effect_power`,
  `effect_npc`, `effect_items`, `player_knowable` и `forbidden_knowledge` (1 уникальное значение на колонку).
  У `visible_signs` 5 значений, по одному на тип фазы. `rumors` во всех 175 строках строится как
  «Говорят: <название события>».

## Выборка

Проверено больше 30 строк во всех файлах и на всех уровнях confidence:
- первая фаза каждого из 35 событий (chronicle_ref и название) плюс все фазы трёх событий;
- все 21 фигура;
- все 8 строк literacy, 5 letter_genre и 8 rumor/profile (кроме того, все 80 rumor/profile сверены скриптом);
- все 26 polities.

Каждую строку сравнивал с цитируемым источником (черновик JSON / строка sqlite / книжные свидетельства / URL).

## Вердикты по файлам

### historical_events/events.csv — approve_with_limits
Сами 35 событий в основном реальные статьи НПЛ 1230–1250. Книжные свидетельства (Экземплярский, Погодин,
Хрусталёв, Карпов, Янин) подтверждают события 1230–1234, 1243 и 1247. Проблемы:
1. Нет колонки `source_refs`. Есть только `source_titles`, и берётся она из первой фазы.
2. `nov_hist_1243_001` «Смерть Варлаама Хутынского» — неверная атрибуция. Преподобный Варлаам Хутынский умер
   в 1192 г. Под 1243 г. летопись сообщает о смерти другого Варлаама, в миру Вячеслава Прокшинича, погребённого
   на Хутыни (книжные свидетельства, строка 36, Экземплярский 667380). Название нужно исправить.
3. `nov_hist_1242_002` «Вызов Ярослава к Батыю» датирован 1242 г. Книжные свидетельства (строки 280, 288;
   Янин 220871, Карпов 556930) дают поездку и ярлык в 1243 г. Дату нужно сверить по НПЛ.
4. `nov_hist_1245_001` «Казнь Михаила Черниговского» под 1245 г. Фактическая дата — 20.09.1246 (строка 262;
   «Русь и монголы» 378072). Если это летописная датировка, так и надо пометить. Иначе ошибка в дате.
5. `nov_hist_1230_004` «Торжокская расправа» — название вводит в заблуждение. В Торжок 8.12.1230 ушли
   Водовик и Ростислав, а убийство Семёна Борисовича и разгромы были в Новгороде (sqlite events 1230-12-08/09,
   книжные свидетельства, строка 6).

### historical_events/event_phases.csv — rework
1. **Содержание фаз — шаблонная болванка.** Эффекты, «что знает игрок» и «запретное знание» одинаковы во всех
   175 строках. Видимые признаки и слухи не относятся к конкретному событию. В таком виде у 175 фаз нет
   исторического содержания для прозы и рантайма, кроме даты, названия и chronicle_ref.
2. **Коллектор отбросил поля черновика, в которых было содержание:** `summary` (175 уникальных),
   `historical_context` (35), `market_effects.goods_affected` (7 вариантов: хлеб, серебро, воск, дерево, соль…),
   `regions_affected`. Потерян и флаг аудита черновика: 5 фаз со `status=needs_review` и примечанием
   «Сверить по русскому тексту эпизоды у Торопца, Жижича и Усвята» (`nov_hist_1245_002_*`) стали обычными
   `candidate/C`, а примечание исчезло (grep «Жижич|Усвят» = 0).
3. `date_range` содержит часы, которые черновик назначил как игровые якоря
   (`assigned_datetime_is_game_anchor=true` у 174/175). Исторической точностью они не являются, но в CSV флаг
   не перенесён: часы вроде `T09:00` выглядят как исторические данные.
4. Повышение до `B` сделано по совпадению ключевых слов с годом, а не по сверке факта. Часть ссылок неверна:
   `nov_hist_1231_002_*` (5 фаз, поход 1231 и смерть Водовика) ссылаются на sqlite-события `1230-лето;1230-12-30`,
   где этих фактов нет. `nov_hist_1230_002/004` ссылаются на `1230-12-30` (возвращение Ярослава) через основу «ярослав».
   По существу 1231_002 верно (книжные свидетельства, строки 15, 16, 241), но ссылки нужно исправить на книжные.
5. README называет `chronicle_ref` «дословной цитатой из перевода Michell & Forbes». На деле это английские
   пересказы, написанные LLM в черновике, например «German/Pskov/Izborsk conflict mentioned in chronicle»,
   а не цитаты и не ссылки на статью или страницу издания.
6. Главное требование брифа (построчная сверка с НПЛ) выполнено на 0%: 14% фаз — только совпадение ключевых слов.
   Файлы НПЛ, на которые ссылается README (`scratchpad/gb-collect-history-events-knowledge/novg*.txt`),
   уже удалены. Готовые книжные свидетельства (184 строки `historical_events` и 66 строк `knowledge_rumors`)
   не использованы совсем: в данных 0 ссылок `book:`.
7. `node_refs_v17` пуст во всех 175 строках. Пробел честно заявлен, но критерий приёмки брифа не выполнен.

### historical_figures/figures.csv — rework
Строки из sqlite (A) верно отражают `persons_1230`. Но окна должностей, которые вычислил скрипт и расширил
по черновику, фактически ошибочны при confidence A:
1. `hf_sql_r08` Степан Твердиславич `office_end=1240-12-31`. Он умер посадником 16.08.1243: книжные свидетельства,
   строки 37, 109, 149, 268, и собственное событие `nov_hist_1243_002`. «1240-е» из черновика превратилось в 1240.
2. `hf_sql_r03` Ярослав Всеволодович — «приглашённый князь» до 1246-12-31. Новгородское княжение — 1231–1236
   (так записано даже в `po_vladimir_suzdal_principality` этой же группы). Умер он 30.09.1246, то есть конец окна
   позже смерти. Это противоречие между доменами.
3. `hf_sql_r04` Фёдор Ярославич `1230-01-01..1233-12-31`: прибыл в конце 1230 г., умер 10.06.1233
   (`nov_hist_1233_002`). `hf_sql_r05` Александр начинается с 1230-01-01, а в источнике «конец 1230».
4. `hf_sql_r09` Микита Петрилович начинается с 1230-01-01, а в источнике «с декабря 1230». Предшественник,
   тысяцкий Борис (Негочевич), бежавший с Водовиком, в таблице отсутствует (книжные свидетельства, строки 7, 17).
5. `hf_sql_r01` Михаил Черниговский `office_end=1230-06-30`: дата придумана скриптом, в источнике «начало 1230»
   и «1230-05/06».
6. `hf_draft_hist_npc_birger_magnusson` назван «шведский ярл» в 1240 г. Это анахронизм: Биргер стал ярлом
   в 1248 г. Его участие в Невской битве спорно (сам коллектор пишет об этом в polities), но в этой строке
   оговорки нет.
7. `hf_draft_hist_npc_batu_khan` — окно 1237–1243 взято без основания (правил до 1255/56).
   `hf_draft_hist_npc_dalmat_archbishop` с 1249-01-01 перекрывает Спиридона: точная дата не установлена,
   а выглядит как установленная.
8. `source_refs` у 7 черновых строк — общая фраза «general scholarly consensus (… Янин; НПЛ)» без конкретного
   места. Это не ссылка на источник.
9. «Михаил Степанич» (D, конфликт) помечен правильно. Рекомендация: исторический посадник Михаил Степанович —
   сын Степана Твердиславича, на посадничестве со второй половины 1250-х, за пределами 1230–1250. В этом окне
   строку как посадника нужно удалить или переатрибутировать как боярского сына; решение за владельцем.
10. `event_refs` пуст во всех 21 строках.

### knowledge_rumors/knowledge_rumors.csv — approve_with_limits
Строки rumor и knowledge_profile (80) перенесены из черновика точно, confidence C верный. Letter_genre (17)
совпадают с sqlite `birchbark_selection`. Ограничения:
1. У 8 строк `literacy` стоит B без конкретной библиографической ссылки: «научный консенсус (Зализняк, Янин)».
   Две строки прямо названы реконструкцией: `kn_literacy_смерды…` («рабочая реконструкция») и
   `kn_literacy_холопы_челядь` («reconstruction by absence», «прямых свидетельств нет»). По правилам им нужен C.
2. Letter_genre — это темы, а не жанры: 17 «пулов» на 19 грамот, почти 1:1. «кредит» и «кредит и счёт»
   разведены по разным пулам. «межгородская грамотность» и «локальная грамотность» — не жанры. README пишет
   «выборка 1220–1240 гг.», но у 12 из 19 грамот датировка 1200–1220.
3. В id `kn_genre_кредит_и_сч_т` потеряна «ё»: нормализатор id режет символ.
4. У `kn_rumor_rumor_tatar_pressure_distant` («татарская/ордынская тревога») нет ограничения по дате.
   Слово «ордынская» для 1230–1236 анахронично: такой слух нужно ограничить 1237+ (или 1223+ без «Орды»).
5. Словари ролей не сведены (свободный текст, `social_group_id`, `occupation_id`). Occupations TSV не
   использован. Книжные свидетельства по `knowledge_rumors` (66 строк: молва, объяснения отступления Батыя и др.)
   не использованы.

### polities_external_relations/polities_external_relations.csv — approve_with_limits
Факты внешних акторов, проверенные поиском, верны: Торжок взят 5.03.1238, поворот у Игнач-креста;
Невская битва 15.07.1240 со спорным командованием; Ливонский орден — май 1237 после Сауле 22.09.1236;
коронация Миндовга 1253; договор с Готским берегом S02. Ограничения:
1. Дублей сущностей пять пар, а не четыре, как в README: Псков, Корела, Белозерье, Заволочье и ещё
   Владимиро-Суздальская земля (`po_region_vladimir_suzdal_land` и `po_vladimir_suzdal_principality`).
2. Колонка `status` содержит описание отношений, а собственно статус лежит в `status_flag`. Схема расходится
   с остальными файлами группы.
3. У `po_region_*` шаблонный `status` («союз/данническая зависимость/эпизодическое военное давление»). Для
   Владимиро-Суздальской земли «данническая зависимость» бессмысленна. Содержания в этих строках почти нет.
4. `po_denmark_estonia`: «прямых сведений о взаимодействии нет» — неточно. Датские эстляндские вассалы
   участвовали в походах 1240–1242 гг. Кроме того, в 1227–1238 гг. Северная Эстония была у меченосцев и
   возвращена Дании в 1238 г. (Стенбю). Пробел помечен D правильно, но формулировку нужно исправить.
5. У `po_lubeck_german_merchants` «ранняя ганзейская сеть» стоит без источника, а термин анахроничен
   для 1230–1250. `po_gotland_german_towns` имеет A: договор первичен, но то, что режим продолжает действовать
   в 1230–1250, — это вывод, и по правилу ему положен B. `tribute_or_treaty_refs` ссылается на «domain
   knowledge_rumors/law», которого в этой группе нет.
6. `po_pskov_land` имеет kind=`city`, хотя это земля. `relation_period` — не машинный формат («1230-е-1250»).
   `confidence` содержит нестандартное значение `A/B`.
7. Нет валидатора. Сверки дат с `historical_events` и `figures` нет: противоречие по Ярославу, см. figures, п. 2.

## Что требуется для доведения (для коллектора следующего прохода)
- Фазы: вернуть поля черновика (`summary`, `historical_context`, `goods_affected`, флаг `needs_review` с
  примечанием, флаг «игровой якорь» для часа), заменить повышение до B по ключевым словам ссылками на
  книжные свидетельства (`book:<id> §… ¶…`), исправить названия и даты событий 1243_001, 1242_002, 1245_001 и 1230_004.
- Фигуры: окна должностей из книжных свидетельств, а не парсинг «1240-е»; добавить Бориса-тысяцкого и
  архиепископа Антония (до 1232 г.); исправить Биргера и Батыя; конкретные ссылки для черновых строк.
- Скрипты: переименовать в `.cjs` (или перевести на ESM), чтобы README был воспроизводим.

## Исправления 2026-09-26

Кто: FIXER-агент (не автор исходной сборки, не верификатор). Правил только 2 файла, помеченных `rework`:
`historical_events/event_phases.csv` и `historical_figures/figures.csv` — плюс их README и build/validate-
скрипты. `historical_events/events.csv`, `knowledge_rumors/*`, `polities_external_relations/*` (статус
`approve_with_limits`, не `rework`) не трогались. Источники: черновик
`data/world-catalogs/novgorod/sources/nov-region-audit-v1/novgorod_historical_timeline_1230_1250_v1.json`
(поля `summary`/`historical_context`/`market_effects.goods_affected`/`status`/`audit_notes`), sqlite
`novgorod_1230(1) (1).sqlite` (свежий экспорт `persons_1230`/`events`), и книжные свидетельства
`servak:/srv/novgorod-work/data/books/evidence/history-events-knowledge.csv` (349 строк, только чтение).

### `historical_events/event_phases.csv`

1. **Прежний кросс-чек по sqlite/ключевым словам заменён книжными свидетельствами.** Старый
   `build_events.js` повышал `confidence` до `B` для любой фазы 1229–1231 гг., пересекающейся по году и
   общему слову с одной из 16 записей sqlite `events`, включая случаи, где совпавшая sqlite-запись не
   описывает эту фазу (пункт 4 верификации: `nov_hist_1231_002`, `nov_hist_1230_002`, `nov_hist_1230_004`).
   Новый `build_events.cjs` даёт `confidence=B` только 5 событиям (25 фазам:
   `nov_hist_1230_001..004`, `nov_hist_1231_002`), где содержание фазы построчно сверено с конкретными
   номерами параграфов книжных свидетельств: `book:667380` (Экземплярский) ¶486/¶487/¶499,
   `book:301539` (Погодин) ¶4366/¶4370. `source_refs` этих 25 строк теперь содержит `book:<id> §<section>
   ¶<para>` вместо ссылки на неподходящую дату sqlite. Остальные 30 событий (150 фаз) остались
   `confidence=C` — книжные свидетельства для них в этом проходе не проверялись (это тот же пробел на
   14%/86%, что и раньше, просто без фиктивного B).
2. **Вернуты потерянные поля черновика** (пункт 2 верификации): добавлены колонки `summary` (175 уникальных
   значений), `historical_context` (35 уникальных на событие), `market_goods_affected` (7 наборов товаров
   вместо одного шаблонного `effect_market` на все строки), `needs_review` + `needs_review_note` (5 фаз
   `nov_hist_1245_002_*` получили обратно `needs_review=true` и примечание черновика «Сверить по русскому
   тексту эпизоды у Торопца, Жижича и Усвята»), `is_game_time_anchor` (174/175 `true` — переносит флаг
   черновика `assigned_datetime_is_game_anchor`, помечая час в `date_range` как игровое допущение, а не
   историческую точность).
3. **`chronicle_ref` переформулирован** (пункт 5 верификации): вместо утверждения «дословная цитата»
   колонка теперь явно говорит «черновой пересказ LLM (не цитата издания и не ссылка на статью/страницу)».
4. **Не исправлено** (вне периметра `event_phases.csv`, наследуется из `events.csv`): названия/даты событий
   `nov_hist_1243_001`, `nov_hist_1242_002`, `nov_hist_1245_001`, `nov_hist_1230_004` — это ошибки
   `events.csv`, который имеет статус `approve_with_limits`, не `rework`; правка — отдельная задача.
   Шаблонность игромеханических полей (`visible_signs`, `rumors`, `effect_roads/power/npc/items`,
   `player_knowable`, `forbidden_knowledge`) также не устранена — это часть игромеханического слоя брифа,
   не историческое содержание (историческое содержание теперь несут восстановленные
   `summary`/`historical_context`/`market_goods_affected`). Построчный аудит НПЛ для оставшихся 150 фаз
   (30 событий) не проведён — вне бюджета этого прохода.
5. Скрипты переименованы `build_events.js` → `build_events.cjs`, `validate_events.js` →
   `validate_events.cjs` (пункт «Скрипты» верификации); команды README обновлены; оба скрипта прогнаны
   после правок (`event_phases.csv`: 175 строк, 25×B/150×C, 5×`needs_review=true`,
   `rejected_report.json`: 0 отказов) — вывод соответствует данным в файле.

### `historical_figures/figures.csv`

1. `hf_sql_r08` Степан Твердиславич: `office_end` 1240-12-31 (артефакт парсинга «1240-е») → 1243-08-16
   (дата смерти в должности; `book:667380` ¶499, `book:378072` ¶478).
2. `hf_sql_r03` Ярослав Всеволодович: `office_end` 1246-12-31 (год смерти, ошибочно взятый за конец
   новгородского княжения) → 1236-12-31, согласовано с окном в `polities_external_relations` этой же
   группы (`book:378072` ¶464, `book:566840` ¶1878).
3. `hf_sql_r04` Фёдор Ярославич: `office_start` 1230-01-01 → 1231-01-01 (книжные свидетельства датируют
   прибытие княжичей-наместников январём 1231, не «концом 1230»); `office_end` 1233-12-31 → точная дата
   смерти 1233-06-10 (`book:667380` ¶489, `book:566840` ¶1890).
4. `hf_sql_r05` Александр Ярославич: `office_start` 1230-01-01 → 1231-01-01, тем же основанием.
5. `hf_sql_r09` Микита Петрилович: `office_start` 1230-01-01 → 1230-12-09 (парсер месяца без дня раньше
   терял «декабря» целиком; теперь день синхронизирован с назначением Степана и с добавленной строкой
   предшественника, см. п. 8).
6. `hf_sql_r01` Михаил Всеволодович Черниговский: `office_end` 1230-06-30 раньше стоял как обычная дата;
   теперь явно помечен как верхняя граница диапазона sqlite-события «1230-05/06» (confidence A), не как
   утверждённый точный день.
7. `hf_draft_hist_npc_birger_magnusson`: анахронизм «шведский ярл» на 1240 г. исправлен — `office`
   переформулирован («шведский полководец, титул ярла — не ранее 1248»); участие в Невской битве осталось
   помечено как спорное (источник сам это отмечает).
8. `hf_draft_hist_npc_batu_khan`: `office_end` 1243-12-31 (правил дольше) очищен — точная дата окончания
   правления не подтверждена книжными свидетельствами группы в этом проходе, не заполнено выдумкой.
9. `hf_draft_hist_npc_dalmat_archbishop`: `office_start` 1249-01-01 оставлен (годовое размещение), но
   теперь с явной оговоркой в `note`, что точный месяц перехода не установлен (пересечение со Спиридоном —
   известный артефакт, не ошибка, уже упоминалось в README, но не в самой строке).
10. `source_refs` 7 черновых строк: общая фраза «general scholarly consensus… no page-level cite» заменена
    честной формулировкой «не найдено постраничной цитаты в книжных свидетельствах группы»; для
    `hf_draft_hist_npc_batu_khan` добавлены найденные книжные цитаты (`book:378072` ¶861, `book:220871`
    ¶393).
11. **Добавлены 2 строки** по требованию верификации, не покрытые ни sqlite, ни черновиком, сourced из
    книжных свидетельств (не выдумано): `hf_book_boris_negochevich` — тысяцкий, предшественник Микиты
    Петриловича, бежал с Водовиком в Чернигов 8 дек. 1230 (`book:667380` ¶486, `book:301539` ¶4366,
    `book:566839` ¶648), confidence B; `hf_book_antony_archbishop` — архиепископ на покое, умер 8 окт. 1232
    (`book:667380` ¶488, `book:566839` ¶643), confidence B. Отдельно отмечено в `note`, что книжная запись
    об убийстве «бывшего посадника Бориса» 9 дек. 1230 (`book:566839` ¶661) не отождествлена с тысяцким
    Борисом Негочевичем — недостаточно оснований приписать ему чужую смерть.
12. `hf_draft_hist_npc_mikhail_stepanich` (D, `candidate-conflict`) — **не изменено**: решение об
    удалении/переатрибуции остаётся за владельцем, как и было рекомендовано верификацией.
13. `event_refs` остался пустым во всех строках — вне периметра этого исправления (уже был зафиксирован как
    пробел в README).
14. Скрипты переименованы `build_figures.js` → `build_figures.cjs`, `validate_figures.js` →
    `validate_figures.cjs`; `validate_figures.cjs` также получил фильтр «на покое» (иначе новая строка
    Антония ложно перекрывалась бы с действующим Спиридоном) и защиту от пустых `office_start/office_end`
    в сравнении дат. Прогон после правок: 23 строки, 0 пропусков `source_refs`, 3 перекрытия (было 5) —
    оставшиеся 3 те же самые, что и раньше признанные допустимыми (Спиридон/Далмат, 2× Михаил Степанич).

### Не исправлено / оставлено владельцу

- `knowledge_rumors/*` и `polities_external_relations/*` — статус `approve_with_limits`, не `rework`; их
  замечания верификации (letter_genre как темы, «ё» в id, анахронизм «ордынская» и т.д.) не входили в
  периметр этой задачи.
- `events.csv` (4 ошибки атрибуции/даты) и полный построчный аудит НПЛ для 150 фаз/30 событий
  `event_phases.csv` — остаются открытыми задачами для отдельных проходов (владелец `events.csv`; историк/
  старшая модель для НПЛ).
- `node_refs_v17` (все 175 строк `event_phases.csv`) и `event_refs` (все 23 строки `figures.csv`) — вне
  периметра, уже задокументированы как осознанные пробелы.

## Повторная проверка 2026-09-26

- Кто: независимый re-checker (старший проход; не автор и не fixer). Данные не правились. Скрипты запускались
  только с выводом в scratch (`gb-fix-history-events-knowledge/recheck/`), поэтому `rejected_report.json` в группе
  не перезаписан.
- Воспроизводимость (скрипт): `build_events.cjs` и `build_figures.cjs` отработали с исходниками из `sources/nov-region-audit-v1`,
  sqlite-дампом и книжными свидетельствами. Результат побайтно совпал с `events.csv`, `event_phases.csv` и
  `figures.csv` (`cmp`). Валидаторы: фазы 175 / 0 без chronicle_ref / 0 дат вне окна / 5 `needs_review` / 0 отказов;
  фигуры 23 / 0 пустых source_refs / 3 перекрытия (Спиридон/Далмат, 2 перекрытия с Михаилом Степаничем).
- Пересчёт (скрипт): фазы 175 строк, 0 дублей `phase_id`, 0 битых строк; confidence B 25 / C 150; `book:`-ссылки
  в 25 строках; ссылок на sqlite-даты 0; `is_game_time_anchor` true 174 / false 1; уникальных значений: summary 175,
  historical_context 35, market_goods_affected 7, effect_* / player_knowable / forbidden_knowledge по 1, visible_signs 5.
  Фигуры 23 строки, 0 дублей; A 14 / B 2 / C 6 / D 1; `event_refs` пуст в 23 строках.
- Выборка: все 25 фаз уровня B сверены с цитируемыми абзацами книжных свидетельств. Плюс по 5 фаз событий 1243_001,
  1242_002, 1245_001 и 1230_004. Все 23 фигуры сверены с книжными свидетельствами и строками sqlite `persons_1230`/`events`.

### historical_events/event_phases.csv — rework (узкий)

Решено: п. 3 (флаг игрового якоря), п. 5 (chronicle_ref честно назван пересказом LLM), п. 4 в части механики.
Повышение до B по ключевым словам убрано, у всех 25 фаз B теперь `book:`-ссылки. Абзацы 667380 ¶486/¶487 и
301539 ¶4366/¶4370 существуют и говорят то, что заявлено. Из п. 2 восстановлены summary, historical_context,
goods, needs_review с примечанием (5 фаз 1245_002).

Осталось:
1. Названия и даты 1243_001 («Смерть Варлаама Хутынского» — неверная атрибуция), 1242_002 (поездка к Батыю по
   книжным свидетельствам — 1243), 1245_001 (казнь — 20.09.1246) и 1230_004 («Торжокская расправа») по-прежнему
   стоят в `event_title` и `date_range` 20 фаз. Довод fixer «это ошибка events.csv» не работает: оба файла
   пишет один `build_events.cjs` из одного черновика, и в фазах эти поля свои. Этого требовал и раздел
   «Что требуется для доведения → Фазы».
2. `nov_hist_1230_004_*` (5 фаз B): собственная ссылка fixer (301539 ¶4366) говорит, что расправа была в Новгороде,
   а заголовок на уровне B по-прежнему «Торжокская расправа». В chronicle_ref — «Dec 8/9 Torzhok actions».
3. `nov_hist_1231_002_*` (5 фаз B): даты 1231-07-01…1231-08-20. Цитируемые источники дают поход «осенью 1231»
   (301539 ¶4370; также 566839 ¶680). Флаг игрового якоря снимает претензию к часу, но не к сезону при уровне B.
4. `nov_hist_1230_002_*`: название «…убийство Иванка» опирается на 667380 ¶486, где об убийстве не сказано.
   Нужна ссылка 301539 ¶4365 («Иванко был убит и брошен в Волхов»).
5. `regions_affected` из п. 2 не восстановлено: в черновике 176 вхождений, колонки в CSV нет.
6. Шаблонность (п. 1) устранена лишь частично. `summary` = шаблонная фраза фазы + `historical_context`, то есть
   35 содержательных значений на 175 строк. Эффекты и знания игрока по-прежнему одно значение на всю таблицу.
   Раз это заявлено как игромеханический слой, допустимо как ограничение, но не как историческое содержание фаз.
7. Покрытие книжными свидетельствами — 5 из 35 событий. Готовые строки по 1233 (смерть Фёдора), 1243 (смерть
   Степана и Варлаама-Вячеслава), 1240–1242 и 1238 не использованы. 150 фаз C — ограничение, не блокер.
   `node_refs_v17` пуст — ограничение.

Для снятия rework достаточно п. 1–5. П. 6–7 — approve_with_limits.

### historical_figures/figures.csv — rework (узкий)

Решено и подтверждено источниками: п. 1 Степан → 1243-08-16 (667380 ¶499, 378072 ¶478); п. 3 Фёдор
1231-01..1233-06-10 (566840 ¶1879/¶1890, 667380 ¶489), Александр с 1231-01; п. 4 Микита с 1230-12-09.
Добавлен Борис Негочевич 1228..1230-12-08 (566839 ¶648, 667380 ¶486) и Антоний †1232-10-08 (667380 ¶488,
566839 ¶643), обоим B по делу. П. 5: у Михаила оговорка о диапазоне. П. 6: Биргер — ярл не ранее 1248, верно.
П. 7: у Батыя конец очищен, у Далмата оговорка. П. 8: ссылки честно переформулированы, 5 строк остаются C
без постраничных ссылок. П. 9 оставлен владельцу, как рекомендовано.

Осталось:
1. `hf_sql_r03` Ярослав: значение 1236-12-31 исторически защитимо и согласовано с polities. Но note искажает
   источник: 566840 ¶1878 даёт «4-е княжение: 1230–1246», а не окно до 1236, и «согласуется» — неправда.
   В note нужно честно указать расхождение датировок: 1236 — по 378072 ¶464, где Александр — князь с 1236;
   1246 — по 566840 ¶1878. Выбор нужно обосновать.
2. `hf_sql_r09` Микита Петрилович: `office_end=1230-12-31` при A ничем не подтверждён. sqlite даёт только
   «с декабря 1230», книжные свидетельства — только избрание. Это тот же дефект, что был у Степана: рантайм
   сочтёт, что тысяцкого после 1230 г. нет. Конец надо очистить или датировать по источнику.
3. `hf_sql_r02` Ростислав: `office_end=1230-12-31` противоречит sqlite («8 декабря ушёл… затем изгнан») и
   собственному note строки r01 («до 8 дек. 1230»). Окно перекрывает Ярослава с 1230-12-30. Валидатор этого
   не ловит, потому что у должностей разные названия.
4. `hf_book_boris_negochevich`, note: 566839 ¶661 говорит о Семёне Борисовиче, бывшем посаднике 1219 г.
   (он уже есть в строке r12), а не об «ином лице по имени Борис». Формулировку нужно исправить.
5. Ограничения, не блокеры: годовые окна `1230-01-01..1230-12-31` у r10–r14. Иван Тимошкинич убит в 1230 г.
   до декабря, а конец окна — 31.12. Окно Александра 1231–1250 сплошное, хотя в 1240/41 он уезжал в Переяславль
   (556930 ¶357/¶365). `event_refs` пуст.

Для снятия rework достаточно п. 1–4 (правка build_figures.cjs + пересборка). П. 5 — approve_with_limits.
