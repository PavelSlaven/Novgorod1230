import { canonicalDigest, createOrdinaryResolutionRef } from '@rus/materialization';
export function ordinaryNoop(request) { return Object.freeze({working_projection:structuredClone(request?.working_projection??{}),write_fragments:[],summary:'ordinary discovery unavailable',player_response_boundary:true}); }
export function currentG6(state) { const id=state?.position?.g6_id??state?.position?.g6_ref; return typeof id==='string'&&id.length?{entity_kind:'g6',entity_id:id}:null; }
export function ordinaryState(a) { return {seeded:a.seeded,density_band:a.density_band,remaining_identity_budget:a.remaining_identity_budget,background_groups:a.background_groups.map(({group_ref})=>group_ref),presence_resolutions:a.presence_resolutions.map(({resolution_ref})=>resolution_ref),closed_observation_scopes:a.coverage_closures.map(({coverage_key})=>coverage_key)}; }
export function validExecution(v) { return v!=null&&typeof v==='object'&&Array.isArray(v.supporting_bases)&&v.candidate_context!=null&&v.identity_budget!=null&&v.mechanics_policy!=null&&typeof v.causal_ref==='string'&&Array.isArray(v.source_refs)&&typeof v.candidate_context.target_ref==='string'; }
export function preparedBasis(group) { return {basis_ref:group.group_ref,state:'prepared_seed',scope_ref:structuredClone(group.scope_ref),prepared_seed_provenance:structuredClone(group.prepared_seed_provenance),functional_buckets:[group.functional_bucket],allowed_admission_classes:structuredClone(group.allowed_admission_classes),permission_refs:structuredClone(group.permission_refs)}; }
export function admissionBases(bases) { return bases.map((basis)=>({...structuredClone(basis),policy:{functional_buckets:structuredClone(basis.functional_buckets),allowed_admission_classes:structuredClone(basis.allowed_admission_classes),permission_refs:structuredClone(basis.permission_refs??[])}})); }
export function candidateForDiscovery({candidateContext,query}) { const {target_ref:targetRef,...candidate}=candidateContext,normalized=normalizeDiscoveryQuery(query); return targetRef==null||normalized==null?candidate:{...candidate,candidate_hint:normalized,coverage_ref:`${candidate.coverage_ref}:${normalized}`}; }
export function bindCommittedSourceIdentity(candidate,sourceRef) { if(candidate?.availability_class!=='context_bound')return candidate; if(typeof sourceRef!=='string'||!sourceRef||sourceRef.trim()!==sourceRef||(candidate.source_ref!=null&&candidate.source_ref!==sourceRef))return null; return {...candidate,source_ref:sourceRef,coverage_ref:`${candidate.coverage_ref}:source:${sourceRef}`}; }
export function presenceTransition({envelope,presence,aggregate,identityKey=null}) { if(!['materialize','absent','no_change','authority_required'].includes(presence.status))return null; const {identity}=envelope; return {kind:'resolve_presence',request_identity:envelope.request.request_id,expected_state_version:aggregate.state_version,resolution_ref:createOrdinaryResolutionRef({scope_ref:envelope.request.scope_ref,candidate_key:identity.candidate_key,coverage_key:identity.coverage_key,context_version:identity.context_version,request_identity:envelope.request.request_id,policy_version:identity.policy_version}),candidate_key:identity.candidate_key,coverage_key:identity.coverage_key,category_key:identity.category_key,context_version:identity.context_version,resolution:presence.status,...(identityKey==null?{}:{identity_key:identityKey})}; }
export function basisDigest(supporting_bases) { return canonicalDigest({domain:'ordinary_supporting_basis_catalog_v1',supporting_bases}); }
function normalizeDiscoveryQuery(value) { if(typeof value!=='string')return null; const normalized=value.normalize('NFKC').trim().replace(/\s+/gu,' ').toLocaleLowerCase('ru-RU'); return normalized.length===0?null:normalized; }
