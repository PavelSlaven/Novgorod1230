# M2c v17: лесная гряда, осмотр и повтор — run 26160

## Результат

**PASS для первого экрана, свободного осмотра, точного повтора и reload; PARTIAL
для M2c.** Обычный game-web и HTTP handler в Chrome, изолированная PostgreSQL
с fixture approvals, release `spatial-v3-production-v17`, сценарий
`novgorod_pine_ridge_approach_v1`, party
`party:5de3fd46e490dda5935c224f`. Тестовый детерминированный provider
проверяет доставку и механику, не качество реальной модели. Production БД не
использовалась.

Первый экран после открытия истории и ACK показал дословно:

```text
НА ПОДХОДЕ К ЛЕСНОЙ ГРЯДЕ

Микула — лесной промысловик.

Вы не ранены; сил достаточно для работы; сильного голода нет.

Различимы очертания деревьев.

нижняя рубаха; штаны; низкая кожаная обувь
```

В UI введено и отправлено ровно: `Осматриваюсь вокруг, оставаясь на месте.`
Игрок увидел:

```text
МЕСТО
Окрестности.
ХОД ЗАНЯЛ
1 мин

Вы внимательно изучили обстановку.

Различимы очертания деревьев.
```

## Фактический исход и readback

Публичный HTTP ход вернул `screen_status=ready`, `turn_number=1`,
`movement=null`. Party `state_version` изменился с 0 на 1, clock
`whole_minutes` с 261120 на 261121; позиция, body rows и число G5 sites (1)
сохранились. Повтор того же POST с теми же `raw_text`, `request_id` и
`idempotency_key` вернул тот же результат без новой записи (`state_version=1`,
время 261121). После reload и «Продолжить» UI показал тот же текст; GET screen
вернул turn 1. Детерминированный PostgreSQL тест fixture: **1/1 PASS**, 0 skipped.

Это исправляет предыдущий FAIL из
[`2026-09-24_m2c-v17-forest-natural-endpoint_1948.md`](2026-09-24_m2c-v17-forest-natural-endpoint_1948.md).
Переход в generated G5, видимость людей и наземных предметов, три семейства,
исчерпание запасов и реальный provider этим прогоном ещё не проверены.
