import test from 'node:test'; import assert from 'node:assert/strict';
import { validateStage17To18HandoffContract, validateStage18To19HandoffContract, validateStage19To20HandoffContract } from '@rus/contracts/time-knowledge-hidden-boundary';
test('Stage 17-19 handoffs require passed audit and permission',()=>{assert.deepEqual(validateStage17To18HandoffContract({version:1,schema:'time_light_consistency_audit',pass:true,commit_permission:{can_continue_to_visible_context:true}}),[]);assert.deepEqual(validateStage18To19HandoffContract({version:1,schema:'character_knowledge_map_audit',pass:true,commit_permission:{can_continue_to_hidden_state:true}}),[]);assert.deepEqual(validateStage19To20HandoffContract({version:1,schema:'full_hidden_state_audit',pass:true,commit_permission:{can_continue_to_visible_context:true}}),[]);});
