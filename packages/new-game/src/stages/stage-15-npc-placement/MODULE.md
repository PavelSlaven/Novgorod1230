# Stage 15 — npc-placement

Code-only Stage 15 materializes NPC instances from approved regional profile sets and G4 rules. The `materialize` port is a code service; LLM is limited to audit and cannot repair or create NPC state.
