# M3 Vikhtuy browser campaign — historical accepted set

## Identity

- Date: 2026-09-20
- PR: #98
- Branch: `codex/live-world-runtime`
- Exact commit: `6bd628773474d8a80346194738eebff9aeb27ae1`
- Scope: M3 N1/S1/O1/A1 browser campaign
- Interface: real Chromium → local game server → disposable PostgreSQL
- Start: `vikhtuy_fishing_camp_v1`, new party
- Gameplay model: `qwen3.8-27b-uncensored-w4a16-tp2`, OpenAI-compatible provider; endpoint and credentials omitted
- WK: mandatory exact/structured + lexical + Giga profile; BGE deferred

## Preconditions

Clean exact checkout. Eight predeclared sequential public turns through ordinary
UI. Campaign required approved TurnScreen narration and retained PostgreSQL
readback; private traces are not reproduced here.

## Gameplay transcript

1. Input: `Внимательно рассматриваю внешность и одежду незнакомого рыбака.`
   Visible result: unknown fisherman in simple clothing repairing tackle by the
   landing. Domain result: N1 detail on the existing background NPC. Commit:
   committed, ready narration.
2. Input: `Осматриваю ближайшее пространство вокруг стана.`
   Visible result: open wooden shelter appeared beside the camp. Domain result:
   one S1 local place. Commit: committed, ready narration.
3. Input: `Захожу внутрь найденной местной постройки.`
   Visible result: movement within the current camp. Domain result: entered the
   persisted local place. Commit: committed.
4. Input: `Выхожу наружу из найденной местной постройки.`
   Visible result: movement within the current camp. Domain result: returned to
   the original base position. Commit: committed.
5. Input: `Ищу среди видимых вынесенных течением ветвей одну обычную ветку.`
   Visible result: `Вы находите «ветку».` Domain result: one O1 ordinary item.
   Commit: committed after 15 minutes.
6. Input: `Ломаю найденную ветку пополам, чтобы получить более короткую ветку.`
   Visible result: one half of the branch shown as shorter. Domain result: A1
   `partial_transformation`, same source identity advanced to state version 2.
   Commit: committed after 5 minutes.
7. Input: `Осматриваю найденную укороченную ветку.`
   Visible result: broken branch and shorter half remained visible after reload.
   Domain result: persisted current state read, no new item. Commit: committed.
8. Input: `Другими словами проверяю состояние той же ветки, не разыскивая новую.`
   Visible result: UI described the held rope rather than the branch. Domain
   readback still contained exactly one ordinary item and no clone. Commit:
   committed.

## Persistence/readback

- N1 remainder rows: 1; S1 resolutions: 1.
- Final journey position: original base position.
- Exactly one `ordinary_item_*` remained.
- A1 retained the same item ID with `partial_transformation`, state version 2.
- Reload/paraphrase created no clone and repeated no materialization effect.

## Findings

- P2 presentation mismatch on turn 8: requested branch inspection, visible prose
  described the held rope. Domain identity/no-clone readback passed, but this
  report does not treat the final player-visible paraphrase as fully accepted.

Superseded for current M3 acceptance by a post-fix exact-head report required by
`AGENTS.md` §24.1.

## Result

`PARTIAL` — persistence, N1/S1/O1/A1 and no-clone evidence passed; turn-8 visible
delivery did not match the requested referent. Exact only for
`6bd628773474d8a80346194738eebff9aeb27ae1`.
