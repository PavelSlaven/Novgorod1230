import { STAGE17_AUDIT_SCHEMA, STAGE17_ROUTE_SCHEMA, STAGE17_ROUTES } from '../policy/constants.js';
import { authoritativeFrame, deepEqual, isObject, issue } from '../../../time-light/shared.js';
export function validateStage17TimeLightAudit(audit,input,precheck){const concerns=[];if(!isObject(audit))return[issue('TIME_LIGHT_AUDIT_INVALID_JSON','Audit must be an object.','root')];if(audit.version!==1||audit.schema!==STAGE17_AUDIT_SCHEMA)concerns.push(issue('TIME_LIGHT_AUDIT_SCHEMA_MISMATCH',`Expected ${STAGE17_AUDIT_SCHEMA} version 1.`,'schema'));if(audit.request_id!==input.request_id)concerns.push(issue('TIME_LIGHT_AUDIT_SCHEMA_MISMATCH','request_id must match input.','request_id',input.request_id,audit.request_id));if(typeof audit.pass!=='boolean')concerns.push(issue('TIME_LIGHT_AUDIT_SCHEMA_MISMATCH','pass must be boolean.','pass'));for(const key of ['authoritative_frame','checks','normalized_visibility_constraints','commit_permission'])if(!isObject(audit[key]))concerns.push(issue('TIME_LIGHT_AUDIT_REQUIRED_BLOCK_MISSING',`${key} is required.`,key));if(!Array.isArray(audit.concerns))concerns.push(issue('TIME_LIGHT_AUDIT_REQUIRED_BLOCK_MISSING','concerns must be an array.','concerns'));if(!Array.isArray(audit.evidence)||audit.evidence.length===0)concerns.push(issue('TIME_LIGHT_AUDIT_EMPTY_EVIDENCE','evidence must be non-empty.','evidence'));if(!deepEqual(audit.authoritative_frame,authoritativeFrame(input)))concerns.push(issue('TIME_LIGHT_AUDIT_CHANGED_AUTHORITY','authoritative_frame must exactly preserve input authority.','authoritative_frame'));if(!deepEqual(audit.normalized_visibility_constraints,precheck.normalized_visibility_constraints))concerns.push(issue('TIME_LIGHT_AUDIT_CHANGED_VISIBILITY_CONSTRAINTS','normalized_visibility_constraints must preserve code precheck output.','normalized_visibility_constraints'));if(audit.pass===true){if(audit.repair_route!=null)concerns.push(issue('TIME_LIGHT_AUDIT_SCHEMA_MISMATCH','repair_route must be null when pass=true.','repair_route'));if(audit.commit_permission?.can_continue_to_visible_context!==true)concerns.push(issue('TIME_LIGHT_AUDIT_PERMISSION_INVALID','can_continue_to_visible_context must be true.','commit_permission.can_continue_to_visible_context'));if(audit.commit_permission?.can_continue_to_narrator!==false)concerns.push(issue('TIME_LIGHT_AUDIT_PERMISSION_INVALID','can_continue_to_narrator must remain false.','commit_permission.can_continue_to_narrator'));}else{if(!Array.isArray(audit.concerns)||audit.concerns.length===0)concerns.push(issue('TIME_LIGHT_AUDIT_SCHEMA_MISMATCH','Failed audit requires concerns.','concerns'));if(!audit.repair_route)concerns.push(issue('TIME_LIGHT_AUDIT_SCHEMA_MISMATCH','Failed audit requires repair_route.','repair_route'));if(audit.commit_permission?.can_continue_to_visible_context!==false||audit.commit_permission?.can_continue_to_narrator!==false)concerns.push(issue('TIME_LIGHT_AUDIT_PERMISSION_INVALID','Failed audit must deny all permissions.','commit_permission'));}return concerns;}
export function validateStage17TimeLightRoute(route){const concerns=[];if(!isObject(route)||route.version!==1||route.schema!==STAGE17_ROUTE_SCHEMA)concerns.push(issue('TIME_LIGHT_ROUTE_INVALID',`Expected ${STAGE17_ROUTE_SCHEMA} version 1.`,'route'));if(!STAGE17_ROUTES.has(route?.route))concerns.push(issue('TIME_LIGHT_ROUTE_INVALID','route is outside the allowlist.','route.route',[...STAGE17_ROUTES],route?.route));if(!Array.isArray(route?.evidence)||route.evidence.length===0)concerns.push(issue('TIME_LIGHT_ROUTE_INVALID','route evidence is required.','route.evidence'));return concerns;}
