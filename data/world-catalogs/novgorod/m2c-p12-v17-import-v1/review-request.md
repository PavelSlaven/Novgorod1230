# M2c P12 v17 import review request

Independent Sol high reviewer: approve or reject the exact [request.json](request.json), SHA-256 `1f19ab21dabcd1cb42a565de6cf5ab079192ff9751377ca6e721ee992ed1b5c4`, at source HEAD `42b0a5444f0d4066a0e6723207ea10b784dfaaa1`. This is a new request for an insert-only import into **`novgorod_world_v17.world_base`** in PostgreSQL cluster `7683141184713630856`. The old P12 request and approval target another database and grant no authority here. No import or activation has occurred under this request.

Fresh-schema [execution attestation](../live-world-runtime-v17/fresh-schema-execution-attestation.json), SHA-256 `04416a846ae1dbbacd3797bed5d1a82bc399cc98fca0d3f49076b66f796091ed`, records `novgorod_world_v17` owned by `world_operator`, 208 `world_base` tables, zero world data rows, and unchanged old databases. Review must bind that exact attestation and its schema readback to this request. Before any import, the operator must recheck live cluster/database identity, schema and emptiness, verify the offline backup, and stop on drift or any existing pinned-key conflict. `novgorod_party_v17`, `novgorod_world`, and `novgorod_party` are outside the write scope.

Review all five manifest and approval SHA-256 pins, importer and effective DDL pins, P12 validation with zero errors and gaps, cross-bundle keys and full rows. Order: expansion, mapped NPC, canonical NPC, acoustic, local movement. Generate each SQL part through `buildTransactionalImportSql({ manifestPath, wrapTransaction: false, allowTypedGaps: false, temporaryTablePrefix })` from the pinned importer. Concatenate UTF-8 `BEGIN;\n`, five returned strings in order, and `COMMIT;\n`, with no other bytes. The regenerated parts and combined SQL matched the pinned byte counts and SHA-256: **109,935,306 bytes**, `bfa0b6960fcf545a1ae637c3043f5c15e48f8b94b772a445108abb821974ba48`. No SQL artifact is committed.

After separate independent approval, execute the exact SQL with only final `COMMIT;` replaced by `ROLLBACK;` against identity-verified `novgorod_world_v17`. Confirm zero errors and conflicts, then read back zero world data rows. Regenerate and rehash the original SQL before its one-transaction COMMIT. Read back all **12,359 distinct pinned rows across 53 tables** with exact full-row equality and the requested per-table counts. Record before/after counts for the two old databases; both must remain unchanged. Any mismatch, failed statement, or nonempty target blocks execution.

Approval and execution attestations belong to independent reviewer and operator. This request grants no import, activation, runtime selection, or party-migration authority.
