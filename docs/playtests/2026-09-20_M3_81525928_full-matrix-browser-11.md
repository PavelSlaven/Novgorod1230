# M3 full-matrix attempt — N1 degraded narration

## Identity

- Date: 2026-09-20
- PR: #98
- Branch: `codex/live-world-runtime`
- Exact commit: `81525928c0fb197e19f763123b0186d3e26811a6`
- Interface: real Chromium → `npm run play:local` → PostgreSQL
- Start: `vikhtuy_fishing_camp_v1`, fresh current-version party
- Gameplay model: `qwen3.8-27b-uncensored-w4a16-tp2`; endpoint/key omitted
- WK: mandatory exact/structured + lexical + Giga

## Preconditions

Fresh party after the shared leave-conversation lifecycle fix and removal of
deprecated authored-save adapters. One fixed full-matrix sequence was
predeclared; the run stopped at its first blocking acceptance result.

## Gameplay transcript

1. Input: `Внимательно рассматриваю внешность и одежду незнакомого рыбака.`
   Visible result: factual recovery UI showed `ВОССТАНОВЛЕННОЕ СОСТОЯНИЕ` and
   only the current place, `рыбацкий стан у Вихтуя`.
   Domain result: N1 factual state committed, but approved narration was not
   delivered. Commit: committed with degraded factual delivery.

## Persistence/readback

The factual result remained recoverable; no claim is made that narration
acceptance passed. No later matrix turn was submitted after this blocker.

## Findings

- P1 acceptance blocker: exact target model writer/auditor path ended in
  `NARRATION_DEGRADED_FACTUAL_DELIVERY` for the first N1 turn. Active contract
  forbids counting factual recovery as narration acceptance.
- O1/A1, conversation/restart, S1, negative and WK matrix portions are not
  claimed by this stopped run.

Superseded for acceptance only by a later post-fix exact-head full-matrix report.

## Result

`FAIL` — exact for `81525928c0fb197e19f763123b0186d3e26811a6`.
