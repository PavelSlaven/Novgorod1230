import { STAGE17_FORMAT_CODES } from '../policy/constants.js';
import { buildStage17TimeLightCodePrecheck } from '../precheck/build-precheck.js';
import { buildStage17SemanticAuditInput } from '../roles/inputs.js';
import { validateStage17TimeLightAudit } from '../validation/audit-validation.js';
import { callRole, routeFailure, stage17Error } from '../shared/runtime.js';
export async function runStage17TimeLightGateBlock({input,audit,formatRepair=null,router=null}={}){const precheck=buildStage17TimeLightCodePrecheck(input);if(!precheck.pass){const route=await routeFailure(router,{input,precheck,audit:null,validation_issues:precheck.concerns});throw stage17Error('Stage 17 code precheck failed.',precheck.concerns,route,{code_precheck:precheck});}let auditOutput=await callRole(audit,buildStage17SemanticAuditInput(input,precheck),'TimeLightSemanticAuditor');let validation=validateStage17TimeLightAudit(auditOutput,input,precheck);if(validation.length>0&&typeof formatRepair==='function'&&validation.some((x)=>STAGE17_FORMAT_CODES.has(x.code))){auditOutput=await callRole(formatRepair,{input,code_precheck:precheck,audit:auditOutput,validation_errors:validation},'TimeLightAuditFormatRepairer');validation=validateStage17TimeLightAudit(auditOutput,input,precheck);}if(validation.length>0){const route=await routeFailure(router,{input,precheck,audit:auditOutput,validation_issues:validation});throw stage17Error('Stage 17 audit output is invalid.',validation,route,{code_precheck:precheck,audit:auditOutput});}if(auditOutput.pass!==true){const route=await routeFailure(router,{input,precheck,audit:auditOutput,validation_issues:auditOutput.concerns});throw stage17Error('Stage 17 semantic audit failed.',auditOutput.concerns,route,{code_precheck:precheck,audit:auditOutput});}return{pass:true,code_precheck:precheck,audit:auditOutput,route:null};}
