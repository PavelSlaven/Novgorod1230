# Stage 19 — Full hidden scene state

Generates and audits hidden scene facts through LLM ports. Code validates stable IDs, approved references, reveal/discovery links, triggers, consequence hooks and the character-knowledge boundary; it never invents hidden facts.

Sibling stage implementations, legacy, provider SDK, DB, UI and server are forbidden.
