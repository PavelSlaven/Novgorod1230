# Stage 17 — Time/light gate

Code-first consistency and semantic-audit boundary for clock, season, weather, light and visibility. The module validates approved upstream facts and never changes world meaning.

Allowed dependencies: `@rus/contracts`, neutral `time-light` helpers and own modules. Provider, DB, UI, server, legacy and sibling stage implementations are forbidden.
