import { deepFreeze } from '@rus/kernel';
const metrics=new Set(['health','satiety','energy']);
export function detectBodyThresholdCrossings({before,after,thresholds=[]}={}){if(!before||!after||!Array.isArray(thresholds))throw new TypeError('body threshold input required');const xs=[];for(const t of thresholds){if(!t||!metrics.has(t.metric)||!['increase','decrease'].includes(t.direction)||typeof t.threshold_id!=='string'||!Number.isFinite(Number(t.value)))throw new TypeError('invalid threshold');const a=Number(before[t.metric]),b=Number(after[t.metric]);if(!Number.isFinite(a)||!Number.isFinite(b))continue;if((t.direction==='decrease'&&a>t.value&&b<=t.value)||(t.direction==='increase'&&a<t.value&&b>=t.value))xs.push({threshold_id:t.threshold_id,metric:t.metric,direction:t.direction,value:t.value,from:a,to:b});}return deepFreeze(xs.sort((a,b)=>a.threshold_id.localeCompare(b.threshold_id,'en')));}
