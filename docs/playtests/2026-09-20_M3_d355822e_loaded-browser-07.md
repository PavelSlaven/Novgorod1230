# M3 loaded-party browser — neutral conversation and local movement

## Identity

- Date: 2026-09-20
- PR: #98
- Branch: `codex/live-world-runtime`
- Exact commit: `d355822e1f37999c876d5cba9df4353d3ea5daaa`
- Interface: real Chromium → restarted `npm run play:local` → PostgreSQL
- Start: loaded `vikhtuy_fishing_camp_v1` party; N1 detail already committed
- Gameplay model: `qwen3.8-27b-uncensored-w4a16-tp2`; endpoint/key omitted
- WK: mandatory exact/structured + lexical + Giga

## Preconditions

Loaded the party from reports 05/06 after fixing neutral phase3 commit admission
and activity dependency pinning. The server was restarted again after the first
successful conversation turn to verify persisted readback before continuing.

## Gameplay transcript

1. Input: `Спрашиваю незнакомого рыбака, как сегодня идёт работа со снастями.`
   Visible result: `Незнакомый рыбак говорит: «Работа обычная, без перемен. Чиню снасти, как и положено.»`
   Domain result: general player/NPC conversation owner committed speech and
   time against the existing NPC. Commit: committed.
2. After server restart/readback, input:
   `После нашего разговора иначе спрашиваю того же рыбака, что теперь изменилось в его работе.`
   Visible result: `Незнакомый рыбак произносит: «Ничего не изменилось. Работа та же, что и была.»`
   Domain result: same persisted NPC and prior conversation context were used;
   no replay of turn 1. Commit: committed.
3. Input: `Осматриваю ближайшее пространство вокруг стана.`
   Visible result: the same fisherman repeated that nothing had changed.
   Domain result: active conversation correctly retained the interaction owner;
   no S1 claim is made for this turn. Commit: committed.
4. Input: `Прекращаю разговор с рыбаком и отхожу осмотреть стан.`
   Visible result: the fisherman ended the conversation. Domain result:
   conversation lifecycle committed. Commit: committed.
5. Input: `Осматриваю ближайшее пространство вокруг стана.`
   Visible result: Miloslav and the same fisherman repairing tackle remained
   visible. Domain result: local observation. Commit: committed.
6. Input: `Захожу внутрь найденной местной постройки.`
   Visible result: movement within the current place. Domain result: entered the
   persisted local place. Commit: committed.
7. Input: `Выхожу наружу из найденной местной постройки.`
   Visible result: movement within the current place. Domain result: returned
   through the same local topology. Commit: committed.
8. Input: `Ищу среди видимых вынесенных течением ветвей одну обычную ветку.`
   Visible result: search result remained unknown. Domain result: honest absent/
   unresolved O1; no item materialized. Commit: committed.
9. Input: `Разрезаю имеющуюся у меня верёвку пополам, сохраняя обе половины.`
   Visible result: prior screen remained; UI reported temporary unavailability.
   Domain result: A1 pre-attempt rejected item mechanics with
   `TRACE_A1_ITEM_MECHANICS_INVALID`. Commit: `not_committed`.

## Persistence/readback

- Turn 1 speech survived a real server restart and was shown unchanged.
- Turn 2 used the same visible NPC and produced a new response, not a replay.
- Conversation termination persisted before local movement.
- Enter/return used the same current place; no canonical route was rewritten.
- O1 unresolved result created no item; failed A1 changed no source.

## Findings

- P1: current A1 rejected the authored rope's committed mechanics before
  execution. This campaign therefore does not establish conservation,
  survivor/detached output or A1 reload.
- P2: turn 3 remained in active conversation despite observation wording; the
  explicit lifecycle turn correctly ended it before S1/local movement.

Superseded for full M3 acceptance only by a later exact-head report that closes
the A1 mechanics finding and remaining required negative/WK matrix.

## Result

`PARTIAL` — neutral NPC contact/restart/continued context and S1 enter/return
passed; O1 was honestly unresolved; A1 failed closed. Exact only for
`d355822e1f37999c876d5cba9df4353d3ea5daaa`.
