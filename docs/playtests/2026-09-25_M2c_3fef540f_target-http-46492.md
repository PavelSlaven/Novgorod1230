# M2c v17: three openings, look, and generated G5

## Identity

- Date: 2026-09-25. PR: #98. Branch at reporting: `codex/live-world-runtime`; checkout HEAD at reporting: `3fef540f53e0a751e398ecc44e1ca4959a5dae3d`. The local JSON does not record its own Git HEAD, so this is not proof of exact run HEAD.
- Primary evidence: local `novgorod-target-http-smoke-46492.json`, `target_http_browser_smoke_v1`, 05:24:36–05:38:17 UTC. Follow-up evidence: `novgorod-target-http-smoke-14364.json`, started 05:43:14 UTC; no `finished_at` recorded.
- Interface: target HTTP browser smoke with isolated PostgreSQL test approvals. Evidence labels browser `Chrome via browser-harness`; this report does not claim a separate manual UI run. Provider: configured local settings; endpoint and credentials omitted.

## Preconditions

- Three requested scenario families: forest (`novgorod_pine_ridge_approach_v1`), riverbank (`novgorod_riverbank_approach_v1`), work/storage (`novgorod_vikhtuy_work_storage_v1`). Each successful opening was acknowledged before its look turn.
- Exact look input throughout: `Осматриваюсь вокруг, оставаясь на месте.`

## Gameplay transcript

Primary run (`46492`); each numbered `submitTurn` below has a ready screen and committed `turn_number`/`state_version` shown in parentheses. Opening calls do not themselves have turn numbers.

1. Forest, party `party:dbeb7a240e8050681fb9306f`: first screen says Mikula is a forest worker on a dry pine ridge in daylight. Input `Осматриваюсь вокруг, оставаясь на месте.` → `Вы внимательно изучили обстановку.` (1/1). The forest route panel has no movement options in this attempt. The exact same turn request/idempotency key was sent again: same turn 1/version 1 and same visible result. `getPartyScreen` returned turn 1 with that prose after the replay.
2. Riverbank: `startNewGame` first returned `AUTHORED_OPENING_AUDIT_REJECTED`; Stage 23 details include `STAGE23_HANDOFF_NOT_APPROVED` and permission-denied codes. Retry used the **same** new-game request ID and succeeded as party `party:697d5fd341e353f1acbd3604`. First screen says Mikula is a boatman; water, shore and trees are visible, with two people nearby whose identity and purpose he does not know. Input `Осматриваюсь вокруг, оставаясь на месте.` → `Вы внимательно изучаете обстановку. Различимы вода, кромка берега и очертания деревьев. В поле зрения находятся два человека, Проход 1 и Продолжить путь — выход 1.` (1/1).
3. Work/storage, party `party:67ccc792618ff4ae29faff63`: first screen says Mikula is a forest worker; water, shore and trees are visible. Input `Осматриваюсь вокруг, оставаясь на месте.` → `Вы внимательно изучаете обстановку. Различаете воду, кромку берега и очертания деревьев. В поле зрения находится Проход 1.` (1/1).
4. Additional forest party `party:fd81f17032de8ff01284ce21`: first screen places Mikula on a dry pine ridge. Input `Осматриваюсь вокруг, оставаясь на месте.` → `Вы внимательно изучили обстановку.` (1/1). This repeats the empty forest route observation; it is not evidence that every forest opening is empty.
5. Forest route party `party:dc1761853fdec81a082076b1`: first screen has trees and an unrecognized person. Input `Осматриваюсь вокруг, оставаясь на месте.` → trees, a person, `Проход 1`, `Продолжить путь — выход 2` (1/1). Input `Проход 1` → local movement completed, `Вы перемещаетесь в пределах текущего места. Окрестности остаются прежними.` (2/2). Input `Осматриваюсь вокруг, оставаясь на месте.` → person, `Проход 1`, `Проход 2`, exit 2 (3/3). Input `Проход 2` → local movement completed (4/4). Input `Осматриваюсь вокруг, оставаясь на месте.` → person, `Проход 1`, exit 2 (5/5). Input `Продолжить путь — выход 2` → route movement completed with `cost_kind: action`, one action unit (6/6); screen shows a person, `Проход 1`, exits 1 and 2, while its prose says the choice result is still unknown. The domain result and prose should be read separately.

Follow-up generated-G5/finite probe (`14364`), separate party `party:9114a42f612a1aaba6e2bc27`: its opening shows forest, trees and one unidentified person. Its turns (input → visible result; turn/version) were:

1. `Осматриваюсь вокруг, оставаясь на месте.` → trees, a person, `Проход 1`, exit 2 (1/1). Exact same request retry returned the same screen and 1/1; `getPartyScreen` read back turn 1.
2. `Проход 1` → local movement within the current place (2/2).
3. `Осматриваюсь вокруг, оставаясь на месте.` → person, `Проход 1`, `Проход 2`, exit 2 (3/3).
4. `Проход 2` → local movement within the current place (4/4).
5. `Осматриваюсь вокруг, оставаясь на месте.` → person, `Проход 1`, exit 2 (5/5).
6. `Продолжить путь — выход 2` → route movement completed (6/6); screen shows trees, a person, `Проход 1`, exits 1 and 2, while prose says the choice result remains unknown.
7. `Осматриваюсь вокруг, оставаясь на месте.` → `Вы стоите на месте и осматриваетесь вокруг. Результат этого осмотра пока не установлен.` (7/7, exact elapsed 0 minutes).
8. `Подобрать одну порцию валежника из доступного запаса.` → no new player screen: `TURN_STEP_PLAN_INVALID`, `turn_commit_status: not_started`, after a planner repair attempt. The model proposed `request_discovery` for one item of the visible finite deadwood source, but the repaired plan did not pass semantic grounding. No pickup or decrement is claimed.

## Persistence/readback

- Primary route party reached a generated site after `arrival → focus → departure → generated G5`: final readback has `site.origin: generated`, one active connection from the prior G5 site, `materialization_runs: 2`, `sites: 2`, two active finite sources (deadwood `60/1`, standing wood `200/1`) and `snapshots: 7`. The source numbers are observed quantities, not proof of depletion.
- Follow-up party likewise has a generated site, one active connection, two sites and two materialization runs. Before and after failed finite input, deadwood remained `60/1` at state version 1, standing wood `200/1`, `finite_decrements: null`, and snapshots remained 8. Failed turn had no commit; this is a typed blocker, not a successful finite pickup.
- Exact turn retry and `getPartyScreen` readback in both evidence files returned turn 1 rather than committing a duplicate. The initial river opening failure and successful same-ID retry are both preserved in the primary evidence.

## Findings

- Primary run completed the three opening/look families and generated-G5 route. Its forest empty-route sample and river unknown-NPC sample are observations of these parties only.
- Follow-up route reached generated G5, but overall acceptance **failed**: the smoke expectation of a whole-minute advance on a look does not match observed zero exact minutes, and the finite pickup stopped on `TURN_STEP_PLAN_INVALID`. The JSON does not prove successful depletion or production deployment.

## Result

`PASS` for primary bounded three-family/generated-G5 smoke (`46492`); `FAIL` for follow-up finite probe (`14364`). Combined M2c finite acquisition acceptance remains blocked by the typed planner error.
