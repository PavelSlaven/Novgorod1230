# M2c v17: повторный UI-прогон лесной гряды — run 74140

## Результат

**BLOCKED на переходе к generated G5.** Обычный game-web в Chrome и
изолированная PostgreSQL с test approvals открыли лесной старт через
`spatial-v3-production-v17`. Настроенный реальный provider работал через
production LLM owner; production БД не использовалась. Party
`party:8fd004cb005e7ae2042955c7`.

Первый экран после выбора «На подходе к лесной гряде», дословная проза:

```text
НА ПОДХОДЕ К ЛЕСНОЙ ГРЯДЕ

Вы — Микула, лесной промысловик. Тело не знает ран, и в нём достаточно сил для работы, а сильный голод не тревожит. На ногах низкая кожаная обувь, на себе штаны и нижняя рубаха. День стоит ясный, и в дневном свете различимы очертания деревьев, которые окружают вас.
```

## Ход и readback

Через UI отправлено ровно `Осматриваюсь вокруг, оставаясь на месте.` Ответ:

```text
Вы внимательно изучаете обстановку, и в полумраке различимы очертания деревьев.
```

Party `state_version` 0→1, clock `whole_minutes` 261120→261121, G5 sites
остался 1. Повторный HTTP POST с теми же `raw_text`, `request_id` и
`idempotency_key` вернул тот же turn 1 и `screen_digest`
`371c168a3469c180998396fa04bf98cff22ca95e77d95e373c80cd758ad1f60b`;
нового хода нет. После reload и «Продолжить» UI показал тот же текст.

Вкладка «Путь» показала «Других известных направлений нет».
`screen.panels.route.data.movement.options=[]`. После подключения
локальных переходов в `b3a59532` видимый выход всё ещё отсутствует.
Переход не выполнялся: игроку нельзя выбрать нераскрытое направление.
Фикстура остановлена с `blocked_local_edge_missing`.

Этот прогон подтверждает первый экран, осмотр, idempotent replay и reload;
не подтверждает переход к generated G5 и оставшуюся UI-матрицу.
