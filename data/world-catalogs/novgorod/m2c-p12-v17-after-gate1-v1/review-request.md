# M2c P12 v17 import after Gate1: independent review request

Independent Sol high reviewer: approve or reject the exact [request.json](request.json), SHA-256 `39471fd4abe829b32f8a7aa468addf69dd7a02845ad09df476a80d296e0e6387`, using source HEAD `4d6a1c1bb67a12965a6874a8951f8ef19da8999c`. This request grants no write. The earlier P12 request (`1f19ab21dabcd1cb42a565de6cf5ab079192ff9751377ca6e721ee992ed1b5c4`) and its approval assumed an empty `world_base`; Gate1 has since committed, so that approval cannot authorize execution now.

Target is `novgorod_world_v17.world_base`, PostgreSQL cluster `7683141184713630856`, owner `world_operator`. Pin the actual [Gate1 readback](../runtime-catalog/gate1-owner-data-v1/v17-bootstrap-import-readback-result.json), SHA-256 `3d91cee04f1e2b8005f7da490c6c42574e929dc9c398001b50cdcfa2835c942c`, state digest `bffa6ed1ffd3082fb5d126740cc73666b43ee2ac77e517a21fe59801de04ec74`. It records 102 approved item templates, 18 container templates, nine G4 sectors, 30 seed tables with 42,577 rows, and unchanged old databases. The fresh-schema evidence records 208 tables; it predates Gate1 and its zero-row claim is historical only.

Review five exact approved bundles in order: expansion, mapped NPC, canonical NPC, acoustic, local movement. Recheck every manifest, approval, dataset, importer and effective DDL pin. Regenerate the five SQL parts through `buildTransactionalImportSql({ manifestPath, wrapTransaction: false, allowTypedGaps: false, temporaryTablePrefix })`; concatenate `BEGIN;\n`, the parts without separators, and `COMMIT;\n`. Expected combined UTF-8 SQL: **109,935,306 bytes**, SHA-256 `bfa0b6960fcf545a1ae637c3043f5c15e48f8b94b772a445108abb821974ba48`. Require zero validation errors or gaps and classify every existing pinned primary key as absent, identical or conflicting by full row. Static source comparison found 17 distinct P12 `source_records` IDs absent from 183 Gate1 seed IDs. `universal_categories` and `record_sources` are outside the Gate1 seed closure. Confirm those facts against the actual target; any conflicting pinned row blocks import. P12 does not write `world_base.world_revisions`.

Before any execution, verify live cluster and database identity, owner, exact 208-table schema, Gate1 row state, all pinned keys, and offline backup. Capture a complete before snapshot of target table counts and persisted payloads, plus old `novgorod_world` and `novgorod_party` counts. Run the exact SQL with only its final `COMMIT;` changed to `ROLLBACK;`; require zero errors and exact before/after equality of every target table. Only after a separate exact approval, regenerate and rehash the same SQL, commit its single transaction to the target only, then read back all **12,359 distinct P12 pinned rows across 53 tables** with full-row equality and per-table counts. Require preservation of every pre-existing Gate1 row and unchanged old databases. Record actual before/after evidence and a separate execution attestation.

Local read-only DB check could not establish the pinned target: the available PostgreSQL container reports cluster `7656105013506273318` and has no `novgorod_world_v17` database. Live pinned-key classification and rollback probe remain mandatory before approval or execution. No SQL artifact is committed; no P12 import, party migration, catalog activation or runtime selection is authorized by this request.
