export async function runG5MaterializationAdapter(input, { materialize } = {}) { return materialize({ input }); }
export async function runG5AuditAdapter(input, { audit } = {}) { return audit({ input }); }
