# Проверка группы «Ремёсла: инструменты, цепочки, мастерские, материалы»

- **Кто:** независимый агент-проверяющий (старший проход). Он не автор этих данных.
- **Когда:** 2026-09-26.
- **Что проверено:** `data/world-catalogs/novgorod/game-base-v1/crafts-tools-processes/` — 11 CSV, README групп и доменов, `scripts/*.cjs`, `build-report.json`, `validation-report.json`.
- **Файлы данных не правились.** Черновые файлы лежали в отдельной временной папке и удалены после проверки.

## Итог

**Общий вердикт: rework (адресный).** Повторный сбор не нужен. Числа из первоисточников, которые я проверил, совпали, выдуманных строк нет. Но у `tools_gear.csv` и `occupation_tools.csv` есть ошибки, которые нельзя принять как есть:

- классы массы у тонких и мягких вещей физически невозможны;
- уверенность у части строк выше, чем у цитируемого источника;
- связи «занятие → инструмент» ссылаются на источник, в котором этих инструментов нет.

Почти всё исправляется правкой правил в `scripts/build.cjs` и `scripts/src/*.cjs` с последующей пересборкой.

| Файл | Строк (скрипт) | README | Вердикт |
|---|---|---|---|
| craft_tools_gear/tools_gear.csv | 157 | 157 | **rework** |
| craft_tools_gear/occupation_tools.csv | 356 | 356 | **rework** |
| craft_processes/processes.csv | 48 | 48 | approve_with_limits |
| craft_processes/process_steps.csv | 166 | 166 | approve_with_limits |
| craft_processes/process_products.csv | 42 | 42 | approve_with_limits |
| workshops/workshops.csv | 21 | 21 | approve_with_limits |
| materials_registry/materials.csv | 103 | 103 | approve_with_limits |
| materials_registry/late_materials_denylist.csv | 27 | 27 | approve_with_limits |
| materials_registry/material_crosswalk.csv | 127 | 127 | approve_with_limits |
| materials_registry/material_resolution.csv | 1498 | 1498 | approve_with_limits |
| sources/sources.csv | 25 | 25 | approve_with_limits |

## Что проверено скриптом

- **Воспроизводимость.** Группа скопирована в изолированное зеркало репозитория. Там запущены `build.cjs` (с `MATCULT_CATALOG`), `crosswalk.cjs` и `validate.cjs` (с `MASTER_TP_DIR`). Результат:
  - `build.cjs`: 0 предупреждений;
  - `validate.cjs`: 27 PASS, 0 FAIL;
  - **все 11 CSV побайтно совпали с файлами в папке.** Значит, данные — чистый результат сборки из `scripts/src`, ручных правок нет.
  - Снимок `material_resolution.csv` на момент проверки тоже совпал.
- **Идентификаторы.** Внутри файлов дублей id нет, полных дублей строк нет, пересечений id между файлами нет. Ключи (occupation_id, tl_id) и (pc_id, step_no) уникальны. `step_count` совпадает с числом шагов у всех 48 цепочек.
- **Ссылки.** Все ссылки `occupation_tools → tools` и `steps → processes` разрешаются. Все `src:*`, `claim:*` и `wk:*` в `source_refs` существуют в `sources.csv` и WK `runtime-bundle.json`: 0 битых. Все 68 занятий есть в TSV, названия совпадают.
- **Обязательные поля.** `source_refs` и `confidence` непусты во всех строках tools, occupation_tools, processes, steps, workshops и materials. У denylist нет колонки `confidence`, у `process_products` нет ни `source_refs`, ни `confidence` (см. ниже).
- **Частоты и веса.** Ни в одном файле группы нет частот или весов, так что выдуманных чисел этого рода нет. Классы массы заданы объявленным правилом, но правило даёт неверный результат (см. tools).
- **Сверка уверенности с источниками.** Для каждой строки уверенность сравнивалась с лучшей из цитируемых. Базовая уверенность источника берётся из `sources.csv`; для WK claim — A, если claim прямой (direct), и B, если выводной (inferred). Строк, где уверенность выше лучшего источника: tools 24, occupation_tools 155, processes 6, steps 23, materials 17 (до учёта `wk_refs` у материалов; конкретные строки ниже). В `occupation_tools` ещё 35 связей имеют уверенность выше, чем у самого инструмента.

## Выборочная сверка с источниками

Проверено около 90 строк во всех файлах и на всех уровнях A/B/C. Открыты:

- Рыбина 2015 (полный текст по URL);
- конспекты Колчина 1959 и Изюмовой 1959 на rezansky.com;
- Колчин 1957 (arheologija.ru);
- страницы КЯЯ 1985 о печатях и о стекле и янтаре;
- страница ИА РАН о буллотирии;
- тексты и evidence цитируемых WK claims;
- строки каталога matcult.

**Подтверждено:**

- **Рыбина 2015.** Блёсны 13–18,5 см, единицы, X–XIII вв.; жерлицы; ботала — более 250, из них 170 из слоёв XI–XIII вв., 175 колец и 76 рогулек; крючки 6–17 см и до 3 см; остроги около 30; стрелы — более 350 в слоях X–XII вв. и около 150 в XIII–XV вв., стержень Ø 0,8–1,2 см; томары около 120; ясеневый лук 87 см, перехват 2,7 см, вторая четверть XIII в.; поплавки из берестяной ленты 8–10 см, плоские около 10×12 см со знаками собственности, деревянные 16–28 см; верша в грамоте № 248; «изредка обнаруживаются устройства для плетения сетей»; бортные крюки с Неревского раскопа.
- **Колчин 1959 (конспект).** Неревский раскоп: 72 топора, 7 тёсел, 8 пил, 28 долот, 15 свёрл, 18 скобелей, 9 резцов, 15 гвоздодёров. Головки молотков 210 и 290 г. Клещи 1350 г при длине 550 мм. Гвоздильня 730 г, 215 мм, 4 отверстия. Ювелирная наковаленка 1460 г. Ювелирные молоточки 33 и 34 г. Напильник 115 мм. Крицы 2650–2920 г, около 140 × 60 мм. Цементация с углём и, судя по натрию в сосуде, с солью.
- **Изюмова 1959 (конспект).** Около 200 колодок из липы и берёзы. Мастерская XIII–XIV вв. — более 2000 фрагментов обуви на 1 м². Ящик для золки XII в. Кожемялка XI в. из Старой Ладоги. Мездрение тёслами-скребками. Сама Изюмова реконструирует технологию по данным XIX в.
- **Колчин 1957.** Не менее 15 кузнечных специальностей, среди них замочники, игольники, ножевники. Перечень деревообрабатывающих инструментов, включая наструги и уторные пилки. Цитата о «мощных, технически совершенных токарных станках» точна. Многослойная сварка лезвий из 3 или 5 пластин.
- **ИА РАН, буллотирий:** длина 20,5 см, колодец XIV в.
- **КЯЯ 1985:** около 2000 печатей, матрицы резали торевты; бусин более 2000; браслеты с 30–40-х гг. XII в., местное производство с конца XII в.; янтаря более 1000 изделий.
- **WK claims:**
  - прямые (direct): saw, axe, ash-hunting-bow, bort-climbing-hooks, weaving-practice, bone-skate, gramota-73;
  - выводные (inferred): ceramic-novgorod-wheel, flax-breaker (Луковец), ploughshare-tillage, pan-lifter, sheep-shearing-scissors, brass (литьё XII в.), tar-pot, bark-tues, pigments (Георгиевский собор, Филиппова), indigo и лак-дай (Нахлик), hay (грамота № 986), plinth-1207.
- **Плотности металлов (CRC):** 7,87; 8,96; 11,34; 7,29; 10,49; 19,3 г/см³ — верны.
- **Анахронизмы в данных не найдены.** Капкан запрещён. Ножной круг, колёсная прялка и отвальный плуг ограничены. Водяная мельница, црен, поливная посуда и эмаль помечены C или как ограниченные.

## Проблемы по файлам

### craft_tools_gear/tools_gear.csv — rework

1. **Правило массы (материал × размер) не учитывает тонкие, длинные и мягкие вещи** и противоречит размерам, записанным в тех же строках:
   - `tl_hunting_arrow` получает M3 (1,5–5 кг), хотя в строке записан стержень Ø 0,8–1,2 см длиной около 1 м: это десятки граммов, M0 (v5 даёт 60 г);
   - `tl_hunting_bow` — M3 (1,5–5 кг) при длине 87 см и перехвате 2,7 см; реально M2;
   - `tl_fishing_rod` — M3 (v5: 200 г);
   - `tl_snare`, `tl_marking_cord`, `tl_plumb_line` и пустой `tl_sack` — M3 по правилу light × soft_goods;
   - носимый оселок `tl_whetstone` — M2 (0,3–1,5 кг) по правилу stone × hand; личный оселок весит десятки граммов, M0–M1;
   - `tl_kneading_trough` (квашня) — M2 при v5 8 кг.

   Нужен модификатор «тонкое или длинное» и «мягкое, пустое» либо поимённые переопределения. После этого — пересборка.
2. **Уверенность выше источника.**
   - 10 строк стоят на A, хотя единственный источник у них — `src:matcult-catalog-v1` (база C по собственному `sources.csv`): `tl_shuttle`, `tl_weaving_sword`, `tl_weaving_tablets`, `tl_potter_paddle`, `tl_pitchfork`, `tl_rake`, `tl_hoe`, `tl_winnowing_shovel`, `tl_sack`, а также `tl_smith_chisel` (Колчин 1957 B + matcult).

     Сборщик перенёс собственное A кандидатного каталога. Это противоречит его же правилу в `sources.csv`: «строка получает confidence не выше своего основного научного источника». Выход: либо цитировать библиографию из `source_ids` matcult, либо понизить до B.
   - Ещё 14 строк стоят на B при единственном источнике matcult (C): `tl_mallet`, `tl_compass_divider`, `tl_anvil_block`, `tl_burnisher`, `tl_beam`, `tl_reel`, `tl_loom_weight`, `tl_clay_trough`, `tl_pottery_stamp`, `tl_hand_quern`, `tl_ice_pick`, `tl_bread_peel`, `tl_kneading_trough`. Туда же `tl_stylus` на A (KYY B + v5 C).
   - На A при выводных WK claims средней уверенности: `tl_whetstone` (claim прямо говорит «выведена косвенно»), `tl_basket_bark` (claim только про туеса), `tl_pan_lifter` (общерусский Колчин 1953) → B.
3. **Атрибуция.**
   - `tl_crucible_tongs` у Колчина — «кричные клещи»: для крицы или тигля, у металлургов и укладников. Название «тигельные клещи» и группа casting_jewelry сужают находку. При этом в `pc_bloomery_iron__s05` крицу достают `tl_smith_tongs`, а не этими клещами.
   - Примечание к `tl_cooper_croze` («пазники» в перечне Колчина 1957) не подтверждается: в тексте «бондарные скобельки, уторные пилки».
   - `tl_stylus` («писала массово известны») и `tl_wax_tablet` (Новгородская псалтирь на церах) ссылаются на `src:kyy1985-applied`. Но сборщик читал только страницы о печатях и о стекле, а там этого нет. Факты верны, ссылка не подтверждает их.
   - `tl_seal_blank_mould` — «отливка вокруг стержня по сводке сайта»: на странице КЯЯ о печатях этого нет.
4. **Количество.** README и отчёт сборщика говорят о 31 инструменте с расхождением класса относительно v5. Повторный прогон `validate.cjs` даёт 30.

### craft_tools_gear/occupation_tools.csv — rework

1. У всех 356 строк `source_refs = src:occupations-v1`, это зашито в `build.cjs`, строка 118. Но в TSV занятий инструменты описаны общими словами: у кузнеца — «специализированный инструмент ремесла, нож, молоток…». Названных инструментов там нет, значит, ссылка связь не подтверждает. Источником должны быть `source_refs` самого инструмента или явная пометка «authored link» с правилом.
2. Уверенность задана на занятие целиком, а не на связь. 35 связей имеют уверенность выше, чем у самого инструмента, например `nov_occ_blacksmith–tl_bellows` A при инструменте B и `nov_occ_carpenter–tl_saw_bow` A при C. 155 строк выше базы C своего единственного источника. Правило: уверенность связи = min(уверенность занятия, уверенность инструмента).
3. По содержанию связи правдоподобны, анахронизмов нет. Все 68 занятий покрыты.

### craft_processes/processes.csv — approve_with_limits

- `pc_charcoal_pit` стоит на A, но WK claim `fuel-pit-charcoal-burning-form`: «Колчин реконструирует … как вероятную форму» (medium, inferred). Правильно B — как у `tl_charcoal_pit` и `ws_charcoal_pit`.
- `pc_weaving` стоит на A только по matcult. Claim об устройстве стана выводной (реконструкция по деталям) → B. `pc_bone_skate` на A допустимо: прямые WK claims высокой уверенности.
- `pc_wax_candle` ссылается на `src:rybina2015-promysly`, но у Рыбиной есть только воск как товар; способ по Гнёздову взят из WK `claim:candle-rolled-plate` (evidence ГИМ, Гнёздово). Ссылку исправить.
- `pc_textile_dyeing`, `pc_lime_burning` ссылаются только на `src:kyy1985-applied`: прочитанные страницы не касаются крашения и извести. Опора — WK claims в `wk_claim_refs`.
- `pc_street_paving` (B, master) можно подкрепить Колчиным 1957: 30 ярусов мостовой Великой улицы, перемощение каждые 20–25 лет (проверено).

### craft_processes/process_steps.csv — approve_with_limits

- 17 шагов стоят выше своей цепочки:
  - `pc_tanning_bark__s02–s04` на A при цепочке B — Изюмова сама реконструирует по данным XIX в.;
  - `pc_pottery_wheel__s01–s03`, `s05`, `s06` на A при B, причём `s06` — обжиг в `tl_kiln` с уверенностью C;
  - `pc_spinning__s02`, `pc_plank_boat__s03–s04`, `pc_wax_candle__s03`, `pc_sewing_mending__s01–s02` — A при B;
  - `pc_bone_comb__s01`, `pc_birch_tar__s01`, `pc_lime_burning__s02` — B при C.

  Шаг не может быть увереннее цепочки, если у него нет собственного источника.
- `source_refs` шага просто копирует источники цепочки; ссылок на уровне шага нет.
- `pc_charcoal_pit__s01–s04` на A → B (как у цепочки).
- `pc_welded_steel_edge__s03` на A: «флюс-песок». Колчин упоминает флюс в общем виде, песок не назван → B или C, либо убрать «песок».

### craft_processes/process_products.csv — approve_with_limits

- Мостовая таблица без `source_refs` и `confidence`; их можно вывести из `produced_by`.
- `items_domain_ref` пуст у всех 42 строк; ждёт доменов предметов.

### workshops/workshops.csv — approve_with_limits

- `ws_net_shore` стоит на A, но археологически засвидетельствованы снасти, а не место починки сетей → B.
- Ссылки на `bt_*_proposed` ждут домена buildings_structures.
- Пять мастерских на C (дегтярня, красильня, стекольная, известковая, восковая) честно помечены как реконструкции.

### materials_registry/materials.csv — approve_with_limits

- **Неверная атрибуция.** `mt_ochre`, `mt_lazurite`, `mt_red_lead`, `mt_carbon_black`, `mt_celadonite` (все A) и `mt_textile_dyes` (A) в `source_refs` ссылаются на `src:kyy1985-applied`. Настоящие источники — исследование живописи Георгиевского собора (Филиппова и др.) и Нахлик. Оба уже есть в WK claims в `wk_refs`, поэтому факты верны, а `source_refs` вводят в заблуждение. То же у `mt_silk`, `mt_gesso` и `mt_textile_generic`.
- **Уверенность выше источника:**
  - `mt_plinfa` на A: WK claim — «вероятная реконструкция» для одного храма 1207 г. → B или C;
  - `mt_hay` на A: грамота № 986, claim medium/inferred → B;
  - `mt_birch_tar` на A: Таланин C + выводной claim о горшках → B;
  - `mt_wood_generic` и `mt_wood_aspen` на A только по USDA (B), что допустимо лишь при учёте `wk_refs`.
- Плотность стали 7,85 г/см³ в `source_refs` опирается только на Колчина; CRC не указан.
- `region_id` пуст у всех строк, то есть все материалы — универсальные категории. Это соответствует модели.

### materials_registry/late_materials_denylist.csv — approve_with_limits

- Нет колонки `confidence`.
- `dl_rubber` и `dl_firearm` подкреплены `src:ethno-analogy`, а это неподходящий тип источника: нужна WK technology-boundaries или библиография. `dl_zinc_metal` ссылается на Десятинный раскоп, который подтверждает только латунь.
- Строка buildings `mat_glass_fragment` («оконное стекло малыми кусками») не срабатывает на `dl_glass_window_common`: основы в `match_stems` её не ловят.
- Предлагаю добавить ветряную мельницу и дистиллированный спирт (водку). Пусть решит утверждающий.

### materials_registry/material_crosswalk.csv — approve_with_limits

- `MAT012` («Бронза/медный сплав») отображён в `mt_bronze;mt_copper`, хотя по WK `metal-classes-composition` нужно `mt_nonferrous_generic`.
- `grass` отображён в `mt_hay`: трава — не сено.
- `MAT015` «рог» отображён в `mt_horn`, хотя речь может идти о лосином роге (`mt_antler`).

### materials_registry/material_resolution.csv — approve_with_limits

- Производный снимок; на момент проверки он воспроизводится.
- 64 строки — полные дубли (31 ключ). Причина: `row_key` для `building_type_parts.csv` берёт только `bt_id` без детали; нужно поправить `validate.cjs`.
- 110 неразрешённых значений принадлежат другим доменам: оружие 63, одежда 22, интерьеры 13, landmarks 12. Их владельцы должны вычистить колонки материалов.

### sources/sources.csv — approve_with_limits

- `src:kolchin1959-iron` и `src:izumova1959-leather` имеют базу A, хотя в `used_for_ru` прямо сказано: «через конспект → B до сверки». То же у `src:sedova1981-jewelry` («через поисковую выдачу»). База должна быть B, пока нет постраничной сверки МИА 65 и Седовой.
- `src:kyy1985-applied` — книга, но сборщик прочёл только две короткие страницы-сводки. Этот источник процитирован 55 раз, в том числе для фактов, которых на этих страницах нет. Нужно либо перечитать книгу, либо переставить ссылки на WK claims.
- У `src:ethno-analogy` нет URL и конкретного источника.

## Пробелы, заявленные сборщиком (подтверждаю)

- МИА 65 и Колчин 1968 постранично не выписаны.
- Нет занятий: литейщик-ювелир, косторез, токарь, стекольщик, сельский металлург, красильщик.
- Длительности заданы качественно.
- 81 категория инструментов предложена как новая.
- Для новых таблиц нужны CR и Contract Auditor.
- `pf`, `bt` и `pr` ждут своих доменов.

## Исправления 2026-09-26

Исправлены только два файла, отмеченные rework выше, и их сборщики. Остальные 9 файлов (`approve_with_limits`) не трогались. `node scripts/build.cjs` (0 предупреждений), `node scripts/crosswalk.cjs` и `node scripts/validate.cjs` (25/25 PASS, 0 FAIL — без `MATCULT_CATALOG`/`MASTER_TP_DIR`; расходится только число информационных проверок, зависящих от этих переменных, не количество ошибок) перезапущены после правок; счётчики строк не изменились (157 и 356).

### craft_tools_gear/tools_gear.csv (`scripts/src/tools.cjs`)

- **Массы тонких/длинных/мягких/пустых вещей** (правило `material_class × size_class` физически не подходило им): добавлено именное переопределение `mass_basis = estimated_physical` в `scripts/build.cjs` (условие `/оценочно/.test(massAtt)`) — класс берётся по обоснованной физической оценке в `mass_attested`, как и по «attested». Исправлены: `tl_hunting_arrow` M3→M0 (~40 г), `tl_hunting_bow` M3→M2 (~800 г), `tl_fishing_rod` M3→M1 (~200 г, как у v5 fishing_line), `tl_whetstone` M2→M0 (~40 г), `tl_snare` M3→M0 (~20 г), `tl_marking_cord` M3→M0 (~40 г), `tl_plumb_line` M3→M1 (~100 г), `tl_sack` M3→M1 (~150 г), `tl_kneading_trough` M2→M4 (~8000 г, как у v5 trough). `info_mass_band_vs_v5_policy` в validate.cjs: расхождений с v5-политикой было 30, стало 27 (часть расхождений — не по этим 9 инструментам, за пределами задания на правку не тронуты).
- **Уверенность выше источника** (правило: строка не выше своего лучшего источника). Понижено A→B (единственный источник — matcult, C, но матчится с уже подтверждённым фактом на уровне B): `tl_shuttle`, `tl_weaving_sword`, `tl_weaving_tablets`, `tl_potter_paddle`, `tl_pitchfork`, `tl_rake`, `tl_hoe`, `tl_winnowing_shovel`, `tl_sack`, `tl_smith_chisel`. Понижено B→C (единственный источник — matcult или v5-item-templates, оба C): `tl_mallet`, `tl_compass_divider`, `tl_anvil_block`, `tl_burnisher`, `tl_beam`, `tl_reel`, `tl_loom_weight`, `tl_clay_trough`, `tl_pottery_stamp`, `tl_hand_quern`, `tl_ice_pick`, `tl_bread_peel`, `tl_kneading_trough`. Понижено A→B (WK claim выводной/inferred, а не прямой): `tl_whetstone`, `tl_basket_bark`, `tl_pan_lifter`; ноты у всех трёх поясняют почему.
- **Атрибуция.** `tl_crucible_tongs`: переименован в «клещи для крицы или тигля» (Колчин называет их «кричными клещами» — годятся и для крицы, и для тигля; заметка теперь честно разводит его с `tl_smith_tongs`, которым в `pc_bloomery_iron__s05` достают крицу). `tl_cooper_croze`: заметка исправлена — у Колчина 1957 «бондарные скобельки и уторные пилки», а не «пазники». `tl_stylus`: `source_refs` заменён с непроверяемого `src:kyy1985-applied` на `claim:residual-v1-birch-writing-used-styli-on-inner-surface` (WK, direct/high) + `src:v5-item-templates`; confidence остаётся A, теперь обоснованно. `tl_wax_tablet`: `source_refs` заменён на `claim:candidate-novgorod-psalter-eleventh-century` (WK, direct/high для самой находки; для 1230 г. вывод по классу, поэтому B — не менялось). `tl_seal_blank_mould`: `source_refs` заменён на `src:iaran-bullotirion`, заметка переписана честно — сама форма в Новгороде не найдена, это реконструкция по механике буллотирия, а не по несверенному первоисточнику Янина; confidence остаётся C.
- **Количество.** README группы обновлён: было «у 76 инструментов есть v5-масса, у 31 расходится класс», теперь 27 (после правок массы выше).

### craft_tools_gear/occupation_tools.csv (`scripts/build.cjs`, генератор в `src/occupation-tools.cjs` не менялся)

- **Ссылки на источник.** Правило в `build.cjs` изменено: `source_refs` связи теперь берётся из `source_refs` самого инструмента (в `tools_gear.csv`), а не из зашитого `src:occupations-v1` — TSV занятий называет инструменты только общими словами и не может подтверждать конкретную связь. Если инструмент не найден — предупреждение сборщика и запасной вариант `src:occupations-v1`. Затронуты все 356 строк.
- **Уверенность выше инструмента и источника.** Правило в `build.cjs`: `confidence` связи = `min(confidence занятия, confidence инструмента)`. Проверено скриптом: строк, где confidence связи выше confidence инструмента, теперь 0 (было 35, включая примеры из проверки: `nov_occ_blacksmith–tl_bellows` A→B, `nov_occ_carpenter–tl_saw_bow` A→C).
- README группы дополнен формулировкой правила `min(occ, tool)` и правила `source_refs`.

### Не исправлено (вне задания на эту правку)

- Остальные 9 файлов группы (`approve_with_limits`): `craft_processes/*`, `workshops/workshops.csv`, `materials_registry/*`, `sources/sources.csv` — не трогались; их проблемы (см. разделы выше) ждут отдельной правки по этому же протоколу.
- `src:kyy1985-applied` цитируется ещё в нескольких строках `tools_gear.csv` за пределами трёх исправленных выше (`tl_bone_rivets`, `tl_bullotirion`, `tl_graver`, `tl_tally_stick` — частично) и в `materials.csv`/`processes.csv` (не в объёме этой правки); их сверка с первоисточником остаётся открытым пробелом.
- Постраничная сверка МИА 65 (Колчин, Изюмова) и Седовой 1981 не проводилась; строки с числами через конспект остаются на B.
