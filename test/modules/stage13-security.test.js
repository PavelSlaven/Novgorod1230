import test from 'node:test';import assert from 'node:assert/strict';import {filterAllowedG5Templates,validateStage13G5MaterializationInput,validateStage13G5SceneGraphDraft} from '@rus/new-game/stages/stage-13/compat';import {canonicalDigest} from '@rus/materialization';import {makeStage13Input,makeG5Draft} from '../fixtures/stage13-16-fixtures.mjs';
for(const [name,patch,code] of [['NPC materialization',{npc_materialization_slots:[{npc_id:'npc-1'}]},'G5_MATERIALIZATION_CREATED_NPC'],['item materialization',{item_materialization_slots:[{item_id:'item-1'}]},'G5_MATERIALIZATION_CREATED_ITEM'],['prose',{intro_prose:'text'},'G5_MATERIALIZATION_CREATED_INTRO_PROSE'],['hidden event',{hidden_event:{id:'e'}},'G5_MATERIALIZATION_CREATED_HIDDEN_EVENT']])test(`Stage 13 rejects ${name}`,()=>assert.ok(validateStage13G5SceneGraphDraft(makeG5Draft(patch),makeStage13Input()).some(x=>x.code===code)));
test('Stage 13 rejects a foreign-region profile even with a recomputed catalog digest',()=>{const input=makeStage13Input();input.allowed_g5_template_set.allowed_g5_templates[0].materialization_profile.region_id='foreign-region';const set=input.allowed_g5_template_set;set.catalog_digest=canonicalDigest({version:set.version,schema:set.schema,selected_g4_type_id:set.selected_g4_type_id,world_revision_id:set.world_revision_id,allowed_g5_templates:set.allowed_g5_templates});assert.equal(filterAllowedG5Templates(input).length,0);assert.ok(validateStage13G5MaterializationInput(input).some(x=>x.code==='G5_MATERIALIZATION_NO_ALLOWED_TEMPLATES'));});
