# M2c v17: лесная гряда с реальной моделью — run 41676

## Результат

**PASS для первого экрана, осмотра, точного повтора и reload; PARTIAL для M2c.**
Обычный game-web и HTTP handler в Chrome, изолированная PostgreSQL с test
approvals, release `spatial-v3-production-v17`, сценарий
`novgorod_pine_ridge_approach_v1`, party
`party:b899c31092b923e6d5e139f7`. Настроенный владельцем
`qwen3.8-27b-uncensored-w4a16-tp2` использован через production LLM owner;
адрес и ключ не сохранялись в отчёте. Production БД не использовалась.

Первый экран после открытия истории и ACK, дословный текст прозы:

```text
НА ПОДХОДЕ К ЛЕСНОЙ ГРЯДЕ

Вы стоите на сухом хребте, где лето 1230 года держит воздух в плотной, светлой тишине. Вы — Микула, лесной промысловик, и ваше тело не знает ран; в плечах и ногах достаточно сил для работы, а в желудке нет того сильного голода, что обычно гонит человека к первой же добыче. На вас штаны, нижняя рубаха и низкая кожаная обувь, и именно в этой простой одежде вы стоите, когда день уже в самом разгаре.

Перед вами, сквозь разреженный свет, различимы очертания деревьев. Они стоят там, где их можно увидеть, и их силуэты не дают никакого ответа на вопрос, почему вы здесь, или что именно вы должны сделать. Нет ни чужих голосов, ни знакомых троп, ни явных целей, которые тянули бы вас в одну сторону или другую. Просто вы здесь, и деревья стоят, и день продолжается.
```

В UI введено и отправлено ровно: `Осматриваюсь вокруг, оставаясь на месте.`
Игрок увидел:

```text
МЕСТО
Окрестности.
ХОД ЗАНЯЛ
1 мин

Вы внимательно изучаете обстановку, и в полумраке различимы очертания деревьев.
```

## Исход и readback

HTTP `submitTurn` вернул `screen_status=ready`, `turn_number=1`, без перемещения.
Party `state_version` 0→1, clock `whole_minutes` 261120→261121; body и позиция
не менялись, число G5 sites осталось 1. Повтор exact POST с теми же
`raw_text`, `request_id`, `idempotency_key` вернул turn 1 без новой записи:
state_version 1, время 261121. После reload и «Продолжить» UI показал тот же
текст. Изолированный PostgreSQL fixture test завершился **1/1 PASS**, 0 skipped.

Открытие длилось 7,4 с у official root, первый ход 15,8 с, повтор 0,2 с.
Полный test занял 336 с вместе с подготовкой БД и действиями в браузере; это
не latency хода.

## Находки и границы

Вступительная проза утверждает отсутствие чужих голосов, знакомых троп и
явных целей. Этот отрицательный факт требует сверки с approved visible package
в Contract Auditor; успешная доставка сама по себе не утверждает его источник.
Переход в generated G5, люди, наземные предметы, три семейства и исчерпание
запаса в этом прогоне не достигнуты.
