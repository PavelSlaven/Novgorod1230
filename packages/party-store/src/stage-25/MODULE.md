# Stage 25 party-schema adapter

Технический адаптер преобразует утверждённые логические цели write plan в текущие физические таблицы party DB. Он не создаёт записи, не изменяет смысл значений и не выполняет SQL.
