# M2c target start — PostgreSQL run 13802

## Identity

- Result: **PARTIAL** for M2c gameplay, **PASS** for the tested start/replay/screen/ACK path.
- Scope: isolated PostgreSQL fixture, `spatial-v3-production-v17`, world revision `novgorod_spatial_v3_target_contract_approval_001`.
- Entry: direct `officialRoot.startNewGame`, then exact replay, `getPartyScreen`, and `acknowledgeOpening`. Audited code is committed in `aadc60c6` and `8ee34d91`; run `13802` captured the pre-commit working-tree bytes.
- Provider: deterministic HTTP fixture for `gameplay_narrator` and `gameplay_narrator_auditor`. No production provider, operational approval or live save was used.

## Preconditions

Fixture-issued exact item/actor approvals and target catalog pins were imported to an isolated real PostgreSQL database. Request: `{ "scenario_id": "novgorod_pine_ridge_approach_v1", "request_id": "target-real-public-opening" }`. Replay used the same identity; ACK used `client_ack_id=target-public-ack`. No player action or turn input followed the opening.

## Gameplay transcript

The first screen returned to the caller, verbatim (including the disabled input panel):

```json
{
  "panels": {
    "route": { "data": { "current_place": "На подходе к лесной гряде" }, "visible": true },
    "character": { "data": { "name": "Микула", "role": "лесной промысловик", "energy": 80, "health": 100, "satiety": 70 }, "visible": true }
  },
  "schema": "first_game_screen",
  "version": 1,
  "party_id": "party:dff60d53bd5e8c692652ec38",
  "main_prose": "Микула — лесной промысловик.\n\nВы не ранены; сил достаточно для работы; сильного голода нет.\n\nРазличимы очертания деревьев.\n\nштаны; низкая кожаная обувь; нижняя рубаха",
  "input_panel": { "input_contract": "gameplay_not_available", "free_text_enabled": false },
  "scenario_id": "novgorod_pine_ridge_approach_v1",
  "action_panel": { "suggested_actions": [] },
  "screen_status": "ready",
  "delivery_state": { "ready": true, "message_id": "opening:party:dff60d53bd5e8c692652ec38" },
  "visible_context": {
    "place": "На подходе к лесной гряде",
    "calendar": "1.7.1230",
    "timestamp": { "whole_minutes": "261120", "subminute_numerator": "0", "subminute_denominator": "1" },
    "environment": { "facts": [] }
  }
}
```

No player turn was submitted, so there is no turn outcome or turn commit to report. The first screen includes no visible NPC, item, finite stock, local exit or rich natural layers; current entity visibility and traversal evidence are still missing.

## Persistence/readback

The test asserted successful committed start, exact request replay, screen readback, opening ACK and canonical Phase2. The factual supplier read current clock/weather in the same transaction; a clock mutation was rejected on recheck. Existing v6 party behavior remained preserved. This does not prove generated G5 first entry, finite stock decrement or cross-G5 turn persistence.

## Timings and model calls

Measured on this run: root startup/catalog 998.323 ms; public start 949.279 ms; HTTP fixture calls 0.106 ms narrator and 0.086 ms narrator auditor. Generation, validation/commit, projection, narration/audit and semantic-remainder timings were not separately exposed by the production facade; no estimates are substituted. Generative **materialization** calls: **0**. Observed model roles were narration and its auditor only.

## Findings

The inherited opening builder hardcodes `gameplay_not_available` and no suggested actions. The root constructs a submit-turn runtime, but no playable action was tested and the screen does not offer input. Current environment contained a calendar and empty weather facts; no hidden or co-located entity was asserted visible merely from location. Earlier failed run: [run 5168](2026-09-24_m2c-target-start_worktree_5168.md).

## Result

**PARTIAL** — official target start/replay/ACK passed in isolated PostgreSQL, while the M2c player-visible full baseline and playable movement acceptance remain open.
