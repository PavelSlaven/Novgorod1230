# M2c finite catalog successor — PostgreSQL run 12852

## Identity

- Result: **PARTIAL** for M2c gameplay, **PASS** for the tested catalog successor and official start.
- Isolated real PostgreSQL fixture for `spatial-v3-production-v17`, target world revision `novgorod_spatial_v3_target_contract_approval_001`.
- Direct `officialRoot.startNewGame` on the working tree later committed as `b45d849e`; run `12852`. The fixture used separately issued test-only approvals. No live production import or party save was touched.
- Provider: deterministic HTTP fixture for narrator and narrator auditor.

## Preconditions

The test compiled 67 target item catalog rows, including the Sol high approved finite-only profile, then performed successor import, activation and exact readback in isolated PostgreSQL. The start request was `{ "scenario_id": "novgorod_pine_ridge_approach_v1", "request_id": "target-real-public-opening" }`. Replay, screen readback and opening ACK followed. No player turn or resource extraction was requested.

## Gameplay transcript

The first screen was returned verbatim:

```json
{
  "panels": {
    "route": { "data": { "current_place": "На подходе к лесной гряде" }, "visible": true },
    "character": { "data": { "name": "Микула", "role": "лесной промысловик", "energy": 80, "health": 100, "satiety": 70 }, "visible": true }
  },
  "schema": "first_game_screen",
  "version": 1,
  "party_id": "party:dff60d53bd5e8c692652ec38",
  "main_prose": "Микула — лесной промысловик.\n\nВы не ранены; сил достаточно для работы; сильного голода нет.\n\nРазличимы очертания деревьев.\n\nштаны; низкая кожаная обувь; нижняя рубаха",
  "input_panel": { "input_contract": "gameplay_not_available", "free_text_enabled": false },
  "scenario_id": "novgorod_pine_ridge_approach_v1",
  "action_panel": { "suggested_actions": [] },
  "screen_status": "ready",
  "delivery_state": { "ready": true, "message_id": "opening:party:dff60d53bd5e8c692652ec38" },
  "visible_context": {
    "place": "На подходе к лесной гряде",
    "calendar": "1.7.1230",
    "timestamp": { "whole_minutes": "261120", "subminute_numerator": "0", "subminute_denominator": "1" },
    "environment": { "facts": [] }
  }
}
```

No player turn was submitted, so there is no turn outcome or turn commit.

## Persistence/readback

The test asserted item successor import/activation/readback, historical party pin preservation, official start/replay/screen/ACK and current environment. A separate finite P16 PostgreSQL test passed rollback, exact property override, source depletion and replay without refilling stock (5/5 focused suite). This official-root run did not itself visit or harvest a generated G5.

## Timings and model calls

Root startup/catalog 924.164 ms; public start 938.303 ms; HTTP fixture narrator 0.114 ms and auditor 0.086 ms. Generation, validation/commit, projection, narration/audit and semantic-remainder stage timings were not separately exposed. Generative materialization calls: **0**. Total test 106,581.6 ms (suite 107,487 ms).

## Findings

The first screen still disables input and provides no visible local movement or entity facts. The compiled finite row is in the isolated activated item catalog; public generated first-entry wiring, live operational approval and gameplay acceptance remain open.

## Result

**PARTIAL** — active-catalog finite policy and official target start proved with test-only approvals; complete generated-G5 play is not yet demonstrated.
