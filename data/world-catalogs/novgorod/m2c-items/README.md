# M2c ordinary items and finite stocks

Status: **independently approved authoring data only; incomplete for import and activation**.
The separate gpt-6-astra/high review approved candidate SHA-256
`55a6b48eda10e80af2bb4db200941a22037697afb4e50941ecfec4709c18868d`
as `APPROVE_M2C_ITEMS_AUTHORING_V1`. The adjacent approval attestation records
the exact decision and its limits. The candidate's pending status is preserved
as the immutable reviewed bytes; approval does not resolve its owner gaps.

`candidate.json` covers all 25 generated G5 families and all 32 exact G4@1 in
`novgorod_spatial_v3_target_contract_approval_001`. The candidate does not change
canonical geography, directional exits, source item rows or active profiles.

The household allocation at the Vikhtuy locality proposes one wooden spoon
and one finite bundle of prepared kindling. Exact
item, quantity, inventory, evidence and property-profile references reuse the
V5 item catalogue. Presence, placement and the one-unit allocation are new
editorial authoring with low confidence; historical attestation of an item type
does not attest a local stock. An exact committed household and occupied-site
basis remain mandatory unresolved inputs. Archaeological settlement evidence
does not establish present occupancy.

Household allocation has an explicit three-way predicate. A committed occupied
household basis with exact property refs admits the rows. A committed
unoccupied, abandoned or non-household classification allocates no rows and
asserts no item absence. Missing or ambiguous classification is a typed gap,
not the false branch. The predicate names candidate adapter inputs; the
data-only artifact does not claim those are existing persistence fields.

The 23 natural/transit families authorize no automatic manufactured items.
That decision is not an absence fact and does not close later ordinary
discovery. Four additional natural-source profiles propose finite reeds,
deadwood, driftwood and harvestable stem wood of a small living tree. They
bind to five matching families through eight exact family/profile bindings.
Their quantities and extraction envelopes are editorial gameplay authoring,
not measurements of local abundance. Prepared kindling remains distinct.

`natural_source_review` records the bounded source investigation. The V5
prepared kindling quantity, scenario prepared-clay finite source and scenario
ambient sand portion supply no transferable raw-wood/reed source. Owner
replies in the current issue #133 task supplied the access decisions for
`cut_reeds`, `gather_deadwood`, `salvage_driftwood` and `cut_standing_wood`,
including pine. `gameplay_access_policy` records this editorial default with
provenance and no historical rights claim. Exact committed parcel/source
property and access rules take precedence; unknown lookup is not permission.
The policy uses the existing `explicit_source_item` basis and never invents
an unowned cause. Access decisions do not approve stock bounds or mechanics.

The initial budgets are 20, 60, 100 and 200 material portions for reeds,
deadwood, driftwood and harvestable small-tree stem wood. Each portion has an
editorial fixed mass of 50 g. One output contains at most 5, 20, 40 or 100
portions respectively. Source and output both use the existing ordinary
admission unit `item`; every quantity is below its limit of 1,000. The initial
masses remain 1,000, 3,000, 5,000 and 10,000 g. Cutting needs validated cutting
capability and a successful
activity; permission grants neither tools, instant felling nor safety.

The existing admission owner independently bounds mass and item quantity; it
does not enforce their fixed relation. The finite adapter verifies equal
source/output item counts only. `M2C_FINITE_FIXED_MASS_OWNER_VALIDATION_REQUIRED`
therefore remains an explicit activation gap: the existing item owner must
derive or validate `mass_grams = quantity.value * 50` from the approved source
profile before creating its immutable snapshot, and P16 must revalidate that
relation. This data correction does not claim the missing check is implemented.

Finite stock is represented once in `party_resource_nodes`; extraction uses
the existing atomic finite-resource transition. It must not also be created
as a baseline item or background group. Source identity includes generated
G5 and the exact profile/row, so repeated entry shares the remainder and a
different G5 receives a separate allocation. Existing explicit allocations
and NPC equipment satisfying the same row take precedence.

The current first-entry implementation accepts one scenario capability and
derives its source identity from party plus authored source. A generic family
reader/adapter and scope identity update belong to that existing owner. This
data-only candidate does not implement or activate them.

Approval must inspect exact source refs, proposed applicability, unresolved
property/occupancy bindings and conservation before import. The target item
catalogue compatibility request is still pending. No import or runtime
readiness is claimed.
