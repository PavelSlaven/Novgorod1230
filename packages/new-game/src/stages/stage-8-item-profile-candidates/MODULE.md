# Stage 8 — item profile candidates

Read-only material profile boundary. В production
`approved_item_catalog_snapshot` строится как pure projection exact verified
runtime import; output сохраняет отдельно domain `source_catalog_digest` и
projection `catalog_digest`. Конкретные предметы, содержимое контейнеров и
инвентари не создаются.
