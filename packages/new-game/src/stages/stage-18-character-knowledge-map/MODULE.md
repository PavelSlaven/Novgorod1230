# Stage 18 — Character knowledge map

Builds and audits the character knowledge boundary. LLM roles create meaning; code validates basis, references, hidden leaks and deterministic write projection.

The stage may import contracts, kernel helpers and its own modules. Sibling stage implementations, legacy, provider SDK, DB, UI and server are forbidden.
