import test from 'node:test';import assert from 'node:assert/strict';import {validateStage15NpcPlacementDraft,validateStage15NpcPlacementAudit} from '@rus/new-game/stages/stage-15/compat';import {makeStage15Input,makeStage15Draft,makeStage15Audit} from '../fixtures/stage13-16-fixtures.mjs';
for(const [key,code] of [['items','NPC_PLACEMENT_CREATED_ITEM_TOO_EARLY'],['dialogue','NPC_PLACEMENT_CREATED_DIALOGUE_TOO_EARLY'],['intro_prose','NPC_PLACEMENT_CREATED_INTRO_PROSE_TOO_EARLY'],['hidden_event','NPC_PLACEMENT_CREATED_HIDDEN_EVENT']])test(`Stage 15 rejects ${key}`,()=>{const d=makeStage15Draft({[key]:key==='dialogue'||key==='intro_prose'?'x':{id:'x'}});assert.ok(validateStage15NpcPlacementDraft(d,makeStage15Input()).some(x=>x.code===code));});
test('Stage 15 audit cannot mutate draft',()=>{const a=makeStage15Audit();a.modified_draft={};assert.ok(validateStage15NpcPlacementAudit(a,makeStage15Draft(),makeStage15Input()).some(x=>x.code==='NPC_PLACEMENT_AUDIT_MUTATED_OUTPUT'));});
